{
  "name": "contracts",
  "root": "contracts",
  "sourceRoot": "contracts",
  "projectType": "library",
  "targets": {
    "materialize": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "node tools/scripts/materialize-openapi-artifacts.mjs"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/run-command-check.mjs --label c-type -- pnpm --dir contracts typecheck"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["typecheck"],
      "options": {
        "command": "pnpm --dir contracts build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/run-command-check.mjs --label contracts-tests -- pnpm --dir contracts test"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/run-command-check.mjs --label contracts-lint -- pnpm --dir contracts lint"
      }
    }
  },
  "tags": [
    "scope:contracts",
    "type:contracts"
  ]
}
