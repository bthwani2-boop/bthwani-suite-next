# MinIO Data Plane

MinIO is not active by default.

## Runtime

- Compose profile: `media`
- Active by default: no

## Rule

MinIO is allowed only when a media slice needs bucket/object storage behavior.