# Restore Decision

Do not restore the full `realtest` agent tree.

Decision:

```text
realtest/.agents = DONOR_REFERENCE
bthwani-suite-next/.agents = COMPACT_CANONICAL_TARGET
```

Allowed extraction:

- concept
- route/gate idea
- corrected rule
- validated small skill pattern

Forbidden extraction:

- wholesale copy
- old root paths
- duplicate adapters
- large generic Nx skill dumps
- `npx` launcher rules
- Graphify-as-leader language
