// Web stub for @expo/vector-icons/Ionicons.
// The real package pulls in expo-font → expo-modules-core → TurboModuleRegistry
// (native-only) and imports .ttf font files that Turbopack cannot process.
// This stub renders nothing; icon rendering on the control-panel web build is
// handled separately if needed.
export default function Ionicons() {
  return null;
}
