# App-field EAS archive rules.
# This runtime may generate native build inputs locally immediately before
# submitting an EAS remote Android build. These files are intentionally not
# tracked, but they must remain available to Expo config during the remote build
# archive.

node_modules/
.expo/
dist/
build/
.tmp/

!google-services.json
!.env.local
