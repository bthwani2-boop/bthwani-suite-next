# App-field EAS archive rules.
# This runtime may generate google-services.json locally immediately before
# submitting an EAS remote Android build. The file is intentionally not tracked,
# but it must remain available to Expo config during the remote build archive.

node_modules/
.expo/
dist/
build/
.tmp/

!google-services.json
