# LeanCTX Adapter

This file is routing only. Canonical repository execution/closure authority for branch `h` is:

`tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`

Load `00` through `05` and applicable `focus/*` owners before using LeanCTX for material `h` work. The conditional LeanCTX tool policy is `.agents/tools/leanctx.md`.

`governance/**` is reconciled Product/System/policy truth input, not a repository execution-rule or closure authority.

LeanCTX may be selected only when it materially reduces repeated reads or noisy context. It cannot suppress exact evidence, required checks, blockers, remaining risks, Stage-A/Stage-B state, `ACTIVE_OPEN_UNIT`, `RECOVERY_FRONTIER`, causal boundaries or decision boundaries, and it owns no repository truth, mutation authority, approval or completion claim.
