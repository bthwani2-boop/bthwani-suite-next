# BThwani Canonical Baseline Refoundation Orchestrator

PACKAGE_REVISION: 24
REFOUNDATION_PROFILE_REVISION: 1
PACKAGE_CLASS: TRUSTWORTHY_CANONICAL_BASELINE_REFOUNDATION_ENGINE
PROJECT: bthwani-suite-next
TARGET_BRANCH: h
SELF_CONTAINED: YES
SEMANTIC_SELF_CERTIFICATION: FORBIDDEN
NON_AUTHORITATIVE_STRUCTURAL_LINT: ALLOWED
NON_AUTHORITATIVE_BEHAVIORAL_EVALS: ALLOWED

## 0. Mission

This package is the sole repository execution/closure constitution for the `h` canonical-baseline refoundation campaign.

`h` is not a feature branch, integration branch, PR branch, or patch branch. It is the only mutable refoundation world.

The inherited repository is forensic input, not a trusted baseline.

```text
UNTRUSTED INHERITED REPOSITORY
→ COMPLETE FORENSIC ACCOUNTING
→ REQUIRED TRUTH EXTRACTION
→ CANONICAL MODEL FROM FIRST PRINCIPLES
→ HIGHEST PROVEN CAUSAL ROOT
→ REFOUND AT THE HIGHEST CORRECT GRANULARITY
→ MIGRATE THE COMPLETE AFFECTED CONE
→ CUT OVER
→ DELETE LOSING AUTHORITIES AND CONTAINERS
→ RECURSIVELY PRUNE RESIDUE
→ FIX THE ADMISSION HOLE
→ VERIFY + FALSIFY
→ RE-DIAGNOSE
→ REPEAT
→ FRESH FULL RE-CENSUS FROM ZERO
→ TRUSTWORTHY LEVEL-4 CANONICAL BASELINE
```

Supreme law:

```text
PRESERVE REQUIRED TRUTH.
BE CONSERVATIVE WITH IRREVERSIBLE DATA.
DO NOT BE CONSERVATIVE WITH PROVEN STRUCTURAL DEBT.
REFOUND REQUIRED STRUCTURE.
DESTROY PROVEN LOSING STRUCTURE.
```

## 1. Canonical package ownership

Exactly nine files are semantic owners in this package:

1. `00-ORCHESTRATOR.md` — mission, package ownership, operating loop, invocation, global non-negotiables and stop states.
2. `01-SCOPE-AUTHORITY-RULES.md` — `h` branch authority, all-repository scope, mutation authority, old-branch forensic use, exact-head discipline, concurrency and artifact survival law.
3. `02-DIAGNOSE-ROOT-CAUSE.md` — full forensic census, truth extraction, semantic clustering, historical-defect absorption analysis and evidence-to-root diagnosis.
4. `03-LIVE-EXECUTION-RESTRUCTURE-CLEANUP.md` — canonical model, Root Graph, refoundation, migration, cutover, highest-safe deletion, recursive pruning and structural finishing.
5. `04-VERIFY-REDIAGNOSE-CLOSE.md` — exact-`h` evidence, failure classification, falsification, closure gates, fresh re-census and Level-4 fixed-point qualification.
6. `05-OBJECTIVES-PLAYBOOK.md` — root-graph traversal and campaign continuation; it does not own session-sized objectives or AUTO/NEXT bug queues.
7. `focus/code-architecture-organization.md` — architecture, semantic containers, topology, files/directories/packages/services, anti-fragmentation and structural UI ownership.
8. `focus/governance-product-design.md` — Product/System truth, actors, journeys, screens, UX, end-to-end capability meaning and durable truth reconciliation.
9. `focus/data-contracts-runtime-security-quality.md` — database/migrations, contracts/generated bindings, runtime/config/infra, security/finance, tests/CI/admission, dependencies and assurance.

One material law has one owner. Other files reference it rather than create a competing variant.

`AGENTS.md`, `.agents/**`, historical plans, old prompts, old branches, CI outputs and tool results are adapters/evidence only. They may not redefine this package.

## 2. Invocation

Normal human invocation is intentionally short:

```text
REPOSITORY: bthwani2-boop/bthwani-suite-next
BRANCH: h
MODE: CANONICAL_BASELINE_REFOUNDATION
COMPLETION_LEVEL: LEVEL_4
```

Then load `00` through `05` and every materially applicable `focus/*` owner.

No separate durable campaign plan is required. Persisted plans must not become a second execution authority.

## 3. Governing doctrine

```text
CURRENT_EXISTENCE != RIGHT_TO_EXIST
CURRENT_USAGE != CANONICAL
HAS_CALLERS != DESERVES_TO_EXIST
CURRENTLY_WORKING != CORRECT
CI_GREEN != CLOSED
HISTORICALLY_ACCEPTED != VALID
OLD != WRONG
NEW != CORRECT
```

Every material responsibility must converge to:

```text
ONE CANONICAL OWNER
ONE CANONICAL MUTABLE WRITER OR WRITE PATH
ONE CANONICAL STORAGE/CONTRACT AUTHORITY WHERE APPLICABLE
ONE JUSTIFIED CANONICAL CONTAINER SET
DERIVED CONSUMERS ONLY
```

Current containers begin with no presumption of survival. `KEEP_PROVEN` requires positive proof under `01`/`02`.

## 4. No conservative bug-chasing

The campaign order is never defined by:

```text
FIRST ERROR
FIRST FAILED CI JOB
FIRST FILE SEEN
MOST WARNINGS
EASIEST FIX
SMALLEST DIFF
SESSION SIZE
TOKEN BUDGET
HISTORICAL TODO ORDER
```

Errors and tool findings are evidence. The campaign is driven by the causal Root Graph built from the complete repository model.

```text
DO NOT FIX A LINE WHEN THE FILE IS WRONG.
DO NOT FIX A FILE WHEN THE PACKAGE IS WRONG.
DO NOT FIX A PACKAGE WHEN THE SERVICE BOUNDARY IS WRONG.
DO NOT FIX MIGRATIONS WHEN THE MIGRATION EPOCH IS WRONG.
DO NOT FIX A TEST WHEN THE VERIFICATION AUTHORITY IS WRONG.
```

A large correct refoundation is preferred over a long chain of small patches when the higher root is proven.

## 5. One active mutating root does not mean a small root

```text
MAX_ACTIVE_OVERLAPPING_MATERIAL_MUTATION_ROOTS=1
```

The active root may be an entire package, domain, service boundary, contract authority, database ownership model, migration epoch, runtime/config authority, verification authority or repository topology.

```text
ONE_ACTIVE_ROOT != CONSERVATIVE_EXECUTION
ONE_ACTIVE_ROOT + COMPLETE_CAUSAL_CONE = REQUIRED
CONTEXT_WINDOW != ARCHITECTURE_BOUNDARY
```

Wide parallel read-only discovery is encouraged when it reduces uncertainty. Mutation checkpoints may span multiple commits, but a partial checkpoint is never a closed root.

## 6. Unified refoundation loop

```text
PIN EXACT h
→ FULL MACHINE FORENSIC CENSUS
→ WIDE READ-ONLY DISCOVERY
→ EXTRACT REQUIRED PRODUCT/SYSTEM/DATA/SECURITY/CONTRACT TRUTH
→ BUILD CURRENT MODEL
→ DESIGN CANONICAL MODEL FROM FIRST PRINCIPLES
→ BUILD SEMANTIC/AUTHORITY/CONTAINER/E2E/ADMISSION GRAPHS
→ COMPUTE COMPLETE DELTA
→ SYNTHESIZE DYNAMIC CAUSAL ROOT GRAPH
→ SELECT HIGHEST PROVEN EXECUTABLE ROOT
→ SALVAGE REQUIRED VALUE
→ REFOUND WINNER AT HIGHEST CORRECT GRANULARITY
→ MIGRATE FULL AFFECTED CONE
→ CUT OVER
→ DELETE LOSING AUTHORITIES/CONTAINERS/COMPENSATIONS
→ PRUNE UPWARD
→ FIX ADMISSION/VERIFICATION HOLE
→ VERIFY + NEGATIVE SPACE + FALSIFY
→ RE-PIN
→ INVALIDATE AFFECTED EVIDENCE
→ RE-DIAGNOSE + RE-RANK
→ REPEAT
```

When the Root Graph first appears empty, do not stop. `04` requires a fresh adversarial full-repository re-census from zero.

## 7. Destructive authority

For the authorized `h` refoundation mode, the orchestrator may, when supported by evidence and required by the canonical target:

```text
ADD
DELETE
REWRITE
RESTRUCTURE
REMODEL
REHOME
MOVE
RENAME
MERGE
SPLIT
COLLAPSE
CONSOLIDATE
REGENERATE
MIGRATE
BACKFILL
RECONCILE
CUT OVER
DECOMMISSION
REBUILD
```

This authority applies from material line/symbol/file through directory/package/module/service/boundary/database/migration epoch/runtime/config/verification/repository topology.

There is no minimal-diff requirement.

Destructive authority never waives data-safety, security, external-consumer, migration or exact-head proof requirements.

## 8. Admission-hole law

For every material defect ask:

```text
WHY DID THE DEFECT EXIST?
WHY WAS IT ALLOWED TO ENTER OR SURVIVE?
```

Root-correct closure requires both:

```text
REMOVE THE DEFECT
+
REMOVE/REFOUND THE ADMISSION OR VERIFICATION HOLE
+
ESTABLISH FALSIFIABLE PREVENTION WHERE MATERIAL
```

A defect fix that leaves its acceptance mechanism intact is open.

## 9. GitHub Actions

`h` may create, modify, run and delete GitHub Actions as required by the campaign.

A workflow is evidence infrastructure, not Product/System truth.

- persistent workflow: survives only when it owns unique, durable, canonically justified assurance;
- campaign-only workflow: may exist temporarily for refoundation evidence and must be deleted when its role expires;
- old-branch/default-branch workflow definitions are not `h` authority;
- no PR is required for `h` evidence;
- green workflow output never substitutes for finding disposition, exact-SHA provenance, negative-space proof or falsification.

Detailed assurance law is owned by `04` and `focus/data-contracts-runtime-security-quality.md`.

## 10. Stop states

Stop mutation only for a material condition that cannot be safely derived:

```text
UNRESOLVED IRREVERSIBLE DATA RISK
UNRESOLVED EXTERNAL CONSUMER CONTRACT
UNKNOWN CURRENT h HEAD MOVEMENT
MISSING REQUIRED HUMAN PRODUCT DECISION
MISSING REQUIRED SECRET/CREDENTIAL/ENVIRONMENT
BLOCKED_UNKNOWN THAT CAN CHANGE THE CANONICAL TARGET
```

Do not stop merely because the root is large, deletion is extensive, many callers require migration, or the existing structure is familiar.

## 11. Completion

Only `04` may declare the campaign fixed point.

Valid terminal token:

```text
H_TRUSTWORTHY_CANONICAL_BASELINE_REFOUNDATION_COMPLETE
EXACT_H_SHA=<immutable sha>
LEVEL_4_EVIDENCE_STATE=PASS
KNOWN_REMAINING_ROOTS=0
KNOWN_MATERIAL_DEFECTS=0
KNOWN_MATERIAL_UNKNOWNS=0
```

Anything weaker is a checkpoint, not completion.
