# Findings details

| Finding | Severity | Root cause | Required outcome |
| --- | --- | --- | --- |
| FND-0001 | P0 | A host-generated LeanCTX activation block was not reconciled with the conditional tool registry. | One canonical lifecycle state and contradiction guard. |
| FND-0002 | P1 | Host adapters evolved without one complete registry-coverage invariant. | Every live adapter registered or explicitly excluded. |
| FND-0003 | P0 | AI routing and broad OSS toolchain catalogs evolved independently. | One writable tool model with generated scoped projections. |
| FND-0004 | P0 | Registration, package aggregation and CI routing are maintained separately. | One fail-closed agent-system aggregate locally and in CI. |
| FND-0005 | P0 | The pinned SHA has no current combined status or workflow-run evidence. | Same-commit required checks for affected agent-system changes. |
| FND-0006 | P0 | Master ruleset 18292744 is disabled and retains stale required contexts. | Reconcile to `BThwani CI result`, then enable with approved controls. |
| FND-0007 | P1 | Tool policies moved to `.agents/tools` but retired skill directories remain. | Migrate consumers, prove zero references, then delete candidates. |
| FND-0008 | P1 | Static wrappers declare tool availability but do not prove target-host execution. | Sanitized Grapify, LeanCTX and OpenCodeReview host evidence. |
| FND-0009 | P1 | No one proof traverses authority → registry → projection → command → CI → external enforcement. | End-to-end same-commit closure and disposal proof. |

## Deletion candidates

- `.agents/skills/graphify`
- `.agents/skills/open-code-review-delegate`

Deletion is forbidden until consumers are migrated, repository references are zero, focused guards pass after the last write, and rollback remains a normal Git revert.
