# 00 — START HERE: `g` CANONICAL RECONSTRUCTION

## Mission

Reconstruct isolated branch `g` into the smallest correct canonical structure that preserves required Product/System/Data truth, restores end-to-end product/data/contract/UI parity, collapses unjustified fragmentation, and eliminates inherited losing containers as well as losing content.

```text
TARGET_BRANCH=g
SCOPE=ALL_MATERIAL_TRACKED_CONTENT_ON_G_ONLY
OTHER_BRANCHES=OUT_OF_SCOPE
PR=FORBIDDEN
MERGE=FORBIDDEN
AUTO_SYNC=FORBIDDEN
PERIODIC_REBASE=FORBIDDEN
FORCE_PUSH=FORBIDDEN
MATERIAL_MUTATION_AUTHORITY=ONE
```

## Authority model

```text
PRODUCT_SYSTEM_DATA_TRUTH=CANONICAL_CORRECTNESS_AUTHORITY
G_RECONSTRUCTION_PLAN=CAMPAIGN_SEQUENCE_DRIVER
ORCHESTRATOR=CROSS_CUTTING_INVARIANTS_ONLY
TRUSTED_CI_TESTS_SCANNERS=EXACT_SHA_EVIDENCE_ONLY
HISTORICAL_PLANS_FINDINGS=EVIDENCE_AND_FALSIFICATION_ONLY
```

The orchestrator does not own pre-census objective discovery, AUTO/NEXT selection, historical-root scheduling, or pre-census root ranking in this campaign.

## Plan order

```text
00 START HERE
→ 01 BRANCH-WIDE CENSUS / LEDGERS / AUTHORITY REGISTRY
→ 02 CANONICAL TARGET / WINNER-LOSER / STRUCTURAL DELTA
→ 02A END-TO-END PRODUCT-DATA-CONTRACT-UI PARITY
→ 02B CANONICAL CONTAINER CONSOLIDATION / ANTI-FRAGMENTATION
→ 03 ROOT SELECTION / CANONICAL RECONSTRUCTION EXECUTION
→ 04 ELIMINATION / DELETION / LINE-FILE-DIRECTORY FINISHING
→ 05 REVERIFY / EXACT-HEAD CI / REDIAGNOSE / REPEAT
→ 06 FINAL ADVERSARIAL QUALIFICATION
```

## Supreme doctrine

```text
PRESERVE REQUIRED TRUTH.
RECONSTRUCT REQUIRED STRUCTURE.
CONNECT EVERY REQUIRED END-TO-END TRUTH CHAIN.
COLLAPSE UNJUSTIFIED FRAGMENTATION.
ELIMINATE EVERYTHING ELSE.
```

`CURRENT EXISTENCE DOES NOT CREATE A PRESUMPTION TO KEEP.`

Every tracked artifact starts as `DEFAULT_DISPOSITION=UNJUSTIFIED_UNTIL_PROVEN`.

Allowed final dispositions:

`KEEP_PROVEN | HARDEN | REFACTOR | REHOME | RENAME | MERGE | SPLIT | REWRITE | REGENERATE | MIGRATE | DELETE | DELETE_NOW | BOUNDED_RETIREMENT`.

There is no final `UNKNOWN`, `NOT_REVIEWED`, `MAYBE`, `LATER`, `KEEP_JUST_IN_CASE`, `HISTORICAL_KEEP`, or permanent temporary state.

## P0 — semantic responsibility outranks names and paths

Do not infer distinct responsibility from distinct names, paths, packages, languages, or surfaces.

```text
SEMANTIC_RESPONSIBILITY > FILE_NAME
SEMANTIC_RESPONSIBILITY > CURRENT_PATH
SEMANTIC_RESPONSIBILITY > CURRENT_PACKAGE
USED != CANONICAL
HAS_CALLERS != DESERVES_TO_EXIST
DIFFERENT_NAME != DIFFERENT_RESPONSIBILITY
DIFFERENT_PATH != DIFFERENT_RESPONSIBILITY
```

Match structural duplication by material responsibility, business meaning, data authority, decision semantics, state transitions, writer/reader behavior, contract meaning, and consumer outcome.

```text
SAME_MATERIAL_RESPONSIBILITY_ACROSS_DIFFERENT_NAMES_PATHS_PACKAGES
= ONE_STRUCTURAL_CLUSTER
```

## P0 — canonical container law

Every material responsibility must have an explicit canonical container map:

```text
SEMANTIC_RESPONSIBILITY
→ CANONICAL_OWNER
→ CANONICAL_PACKAGE_OR_BOUNDARY
→ CANONICAL_DIRECTORY
→ MINIMUM_NECESSARY_CANONICAL_FILE_SET
→ CANONICAL_SYMBOLS
```

The target is `MINIMUM_NECESSARY_FILES`, not `MINIMUM_POSSIBLE_FILES`. Do not create God files. Do not preserve unjustified fragmentation.

A file may survive only if it owns a unique cohesive responsibility or is a justified part of the minimum necessary canonical file set.

```text
LOSING_SEMANTICS_REMOVED + LOSING_FILE_WITHOUT_UNIQUE_RESPONSIBILITY_SURVIVES = ROOT_OPEN
LOSING_FILE_DELETED + RESPONSIBILITY_LESS_PARENT_DIRECTORY_SURVIVES = ROOT_OPEN
```

## P0 — highest safe deletion granularity

Never stop automatically at the first dead line/symbol found. Before deletion, walk upward and delete/collapse at the highest safe canonical granularity:

```text
DEAD LINE?
→ CAN THE SYMBOL DIE?
→ CAN THE FILE DIE OR BE ABSORBED?
→ CAN THE DIRECTORY DIE OR BE COLLAPSED?
→ CAN THE PACKAGE/BOUNDARY DIE OR BE CONSOLIDATED?
→ DELETE AT HIGHEST SAFE CANONICAL GRANULARITY
```

After every deletion/consolidation:

`RE-EVALUATE PARENT → REPEAT UPWARD UNTIL THE FIRST JUSTIFIED CANONICAL CONTAINER`.

## P0 — destructive canonicalization / elimination mandate

```text
ONE MATERIAL MEANING
→ ONE CANONICAL OWNER
→ ONE CANONICAL WRITER
→ ONE CANONICAL CONTRACT/STORAGE TRUTH WHERE APPLICABLE
→ ONE JUSTIFIED CANONICAL CONTAINER SET
→ DERIVED CONSUMERS ONLY
→ ZERO LOSING AUTHORITY
→ ZERO LOSING CONTAINERS
→ ZERO MATERIAL RESIDUE
```

Any proven duplicate/parallel/shadow authority requires winner/loser treatment. Winner/loser applies to semantic authorities and to files, directories, packages, routes, screens, APIs, contracts, generated/manual mirrors, runtime/config authorities, and storage authorities.

## P0 — vertical end-to-end parity mandate

Backend and frontend may be separate layers. They may not become separate product truths.

For every material capability/journey, prove one continuous canonical chain where applicable:

```text
PRODUCT MEANING
→ ACTOR / JOURNEY / STATE / TRANSITION
→ DOMAIN OWNER
→ DATABASE / STORAGE TRUTH
→ BACKEND SERVICE / USE CASE
→ API / EVENT
→ CANONICAL CONTRACT
→ GENERATED BINDING
→ FRONTEND DATA CONSUMER
→ VIEW MODEL / COMPONENT / SCREEN
→ USER ACTION
→ MUTATION
→ PERSISTED READBACK
→ UPDATED USER-VISIBLE STATE
```

Any unexplained break is `PARITY_GAP`.

`CLEAN_BACKEND + CLEAN_FRONTEND + BROKEN_SEMANTIC_CHAIN = NOT_CLOSED`.

## Fast garbage deletion law

Deletion ceremony is proportional to risk. If an artifact is proven unused, unconsumed, unreachable, non-authoritative, not required by the current product/system or supported consumer, and has no state/contract/runtime/security/migration role, delete it immediately — but first evaluate whether a higher parent container can also be removed safely.

```text
PROVEN_UNUSED_AND_NOT_REQUIRED=DELETE_NOW
PROVEN_LOW_RISK_GARBAGE=DELETE_NOW
DELETE_AT_HIGHEST_SAFE_CANONICAL_GRANULARITY=REQUIRED
NO_KEEP_JUST_IN_CASE
NO_DEFERRED_GARBAGE_BACKLOG
```

Before classifying backend/API/data artifacts unused, search all apps/surfaces, control panel, mobile clients, runtime calls, integrations, events/webhooks, scripts/tools, operations, and supported external consumers.

## Pre-mutation baseline and the single exception

Structural, semantic, ownership, schema, contract, runtime, migration, rename/rehome, parity repair, consolidation, and root-correct mutations remain forbidden until the complete baseline passes.

The only pre-baseline mutation exception is low-risk `FAST_GARBAGE_ELIMINATION`, including safe upward pruning when parent containers are proven responsibility-less and low-risk.

## Mandatory branch-wide reconstruction baseline

Before the first non-garbage structural mutation, all stages must pass on one exact pinned `g` SHA:

```text
STEP 0  PIN EXACT LIVE g
STEP 1  FULL TRACKED-TREE CENSUS
STEP 2  SEMANTIC AUTHORITY CENSUS — INDEPENDENT OF NAME/PATH
STEP 3  ARTIFACT RIGHT-TO-EXIST + DISPOSITION LEDGER
STEP 4  FILE + DIRECTORY + PACKAGE RESPONSIBILITY VERDICTS
STEP 5  BUILD COMPLETE CURRENT g
STEP 6  DEFINE COMPLETE CANONICAL g
STEP 7  IDENTIFY WINNING/LOSING AUTHORITIES
STEP 8  COMPUTE COMPLETE CURRENT→CANONICAL STRUCTURAL DELTA
STEP 9  BUILD COMPLETE END-TO-END CAPABILITY/JOURNEY PARITY MATRIX
STEP 10 BUILD CANONICAL CONTAINER MAP
STEP 11 COMPUTE CONSOLIDATION / ANTI-FRAGMENTATION DELTA
STEP 12 RESOLVE/CLASSIFY PARITY + CONTAINER GAPS INTO STRUCTURAL DELTA
STEP 13 SYNTHESIZE/RANK TRUE CAUSAL ROOTS
STEP 14 SELECT HIGHEST PROVEN EXECUTABLE STRUCTURAL ROOT
ONLY THEN FIRST STRUCTURAL/SEMANTIC ROOT MUTATION
```

Before root selection require:

```text
UNREVIEWED_TRACKED_ARTIFACTS=0
UNDISPOSITIONED_MATERIAL_ARTIFACTS=0
UNRESOLVED_FILE_VERDICTS=0
UNRESOLVED_DIRECTORY_PACKAGE_VERDICTS=0
UNRESOLVED_SEMANTIC_AUTHORITIES=0
UNMAPPED_MATERIAL_CAPABILITIES=0
UNRESOLVED_E2E_PARITY_GAPS=0
UNRESOLVED_CANONICAL_CONTAINER_MAP_ENTRIES=0
UNRESOLVED_FRAGMENTATION_CLUSTERS=0
```

## No preservation bias

Never retain wrong or fragmented structure because of smaller diff, familiar path, current callers, existing internal API, compatibility just-in-case, possible future use, cost of updating imports, or fear of deleting many files.

## Migration shells forbidden by default

Internal re-export-only, forwarder-only, shim-only, alias-only, and pass-through files preserving old paths are forbidden final states unless a bounded external compatibility proof requires them.

`UPDATE ALL INTERNAL CONSUMERS → DELETE OLD FILE/PATH/ALIAS`.

## High-risk topology and integration zones

`core/**` and `shared/**` require heightened justification. `infra/**`, `tools/**`, `.github/**`, scripts, workflows, runtime/config, tests, fixtures, mocks, dependencies, database/schema, API/contracts/generated bindings, frontend data layers, components, routes, and screens are first-class structural surfaces.

Generated code may legitimately contain many files only when they are derived from one proven canonical source. Database normalization may legitimately require multiple tables when they represent distinct normalized responsibilities; multiple mutable storage authorities for the same business meaning are forbidden.

## Exact-HEAD and CI law

Before every material push require `EXPECTED_REMOTE_G_SHA == ACTUAL_REMOTE_G_SHA`. Never force-push unseen work. CI remains evidence only: `CI_FAILURE != EXECUTION_ROOT` and `GREEN_CI != CLOSED`.

## Final success meaning

```text
UNREVIEWED_TRACKED_ARTIFACTS=0
UNDISPOSITIONED_MATERIAL_ARTIFACTS=0
UNRESOLVED_FILE_VERDICTS=0
UNRESOLVED_DIRECTORY_PACKAGE_VERDICTS=0
UNRESOLVED_SEMANTIC_AUTHORITIES=0
UNRESOLVED_CANONICAL_CONTAINER_MAP_ENTRIES=0
UNRESOLVED_FRAGMENTATION_CLUSTERS=0
RESPONSIBILITY_LESS_FILES=0
REDUNDANT_FILES=0
UNJUSTIFIED_FILE_FRAGMENTATION=0
REEXPORT_ONLY_INTERNAL_COMPAT_FILES=0
PASS_THROUGH_ONLY_FILES=0
SHIM_ONLY_FILES=0
EMPTY_OR_RESPONSIBILITY_LESS_DIRECTORIES=0
LOSING_CONTAINERS_REMAINING=0
UNRESOLVED_E2E_PARITY_GAPS=0
PARALLEL_TRUTHS=0
SHADOW_AUTHORITIES=0
DUPLICATE_MUTABLE_WRITERS=0
PROVEN_LOW_RISK_GARBAGE_REMAINING=0
KNOWN_NONCANONICAL_TOPOLOGY=0
FINAL_CURRENT_CANONICAL_STRUCTURAL_DELTA=0_UNRESOLVED_MATERIAL_ITEMS
END_TO_END_PRODUCT_DATA_CONTRACT_UI_PARITY=PASS
```

Only then may `G_RADICAL_CLEANUP_COMPLETE` be emitted.
