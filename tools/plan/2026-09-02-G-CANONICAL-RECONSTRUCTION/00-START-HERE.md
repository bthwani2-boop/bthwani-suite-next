# 00 — START HERE: `g` CANONICAL RECONSTRUCTION

## Mission

This is the execution entry for the isolated branch `g` radical cleanup and deep reconstruction campaign.

```text
TARGET_BRANCH=g
SCOPE=ALL_MATERIAL_TRACKED_CONTENT_ON_G_ONLY
OTHER_BRANCHES=OUT_OF_SCOPE
PR=FORBIDDEN
MERGE=FORBIDDEN
AUTO_SYNC=FORBIDDEN
PERIODIC_REBASE=FORBIDDEN
MUTATION_AUTHORITY=ONE
```

## G campaign authority model

For this isolated `g` reconstruction campaign, **this plan owns campaign execution sequencing only**.

The live orchestrator remains the canonical source of applicable cross-cutting execution invariants, but it does **not** drive the campaign traversal order before the reconstruction baseline is established.

```text
G_RECONSTRUCTION_PLAN_ROLE=CAMPAIGN_SEQUENCE_DRIVER
ORCHESTRATOR_ROLE=CROSS_CUTTING_INVARIANTS_SAFETY_VERIFICATION_CLOSURE
TOOLS_TESTS_SCANNERS_ROLE=EVIDENCE_ONLY
HISTORICAL_PLANS_FINDINGS_ROLE=EVIDENCE_ONLY
```

### What this plan owns

This plan owns only the `g`-specific campaign sequence:

```text
PIN LIVE g
→ COMPLETE BRANCH-WIDE CENSUS
→ BUILD CURRENT g
→ DEFINE CANONICAL g
→ COMPUTE STRUCTURAL DELTA
→ SYNTHESIZE TRUE CAUSAL ROOTS
→ SELECT HIGHEST CAUSAL CLOSURE UNIT
→ RECONSTRUCT / MIGRATE / CUT OVER / DELETE
→ VERIFY / RE-PIN / RE-DIAGNOSE
→ REPEAT
→ FRESH BRANCH-WIDE RE-CENSUS
→ FINAL g QUALIFICATION
```

It therefore owns:

- campaign sequencing;
- census order and completeness gate;
- `CURRENT g` model construction;
- `CANONICAL g` target construction;
- Current→Canonical Structural Delta construction;
- the requirement that root synthesis occurs only after the complete initial delta;
- reconstruction/cleanup campaign traversal;
- the final `g` fixed-point sequence.

This plan does **not** become Product/System/Data truth, durable governance, repository-wide authority outside `g`, or a parallel owner of engineering/security/delivery policy.

### What the orchestrator provides in this campaign

Load the latest live `tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`, routed `01–05`, and materially applicable focus modules **only for their applicable cross-cutting invariants**, including:

- Product/System truth and canonical-authority discipline;
- actual Source-of-Defect / Source-of-Fix proof;
- canonical owner/writer proof;
- affected-cone completeness;
- migration/backfill/reconciliation/cutover safety;
- exact-HEAD and concurrent-ref safety;
- evidence provenance/invalidation;
- no parallel/shadow truth;
- verification, negative-space, falsification, and closure invariants;
- `GREEN != CLOSED`.

The orchestrator does **not** own for this campaign:

```text
INITIAL_OBJECTIVE_DISCOVERY
AUTO_NEXT_SELECTION
PRE_CENSUS_ROOT_SELECTION
PRE_CENSUS_ROOT_RANKING
HISTORICAL_ROOT_SCHEDULING
GENERIC_CAMPAIGN_TRAVERSAL_ORDER
SESSION_SIZED_ROOT_SELECTION_BEFORE_CENSUS
```

Do not invoke those generic sequencing behaviors to bypass this plan's required reconstruction baseline.

### Conflict rule

If a generic orchestrator sequencing rule would cause material mutation, root selection, AUTO/NEXT selection, or historical-finding execution **before** this plan completes:

`CENSUS → CURRENT MODEL → CANONICAL TARGET → STRUCTURAL DELTA → CAUSAL ROOT SYNTHESIS`,

then for this isolated campaign:

`G_RECONSTRUCTION_PLAN_SEQUENCE_WINS`.

This exception applies **only to campaign sequencing**. It does not override Product/System/Data truth, safety, migration integrity, security, evidence, or closure invariants.

### Authority precedence for `g`

Use this interpretation:

```text
CURRENT HUMAN INTENT = defines this campaign's authorized goal and sequencing exception
PROVEN PRODUCT/SYSTEM/DATA TRUTH = defines what is materially correct
G RECONSTRUCTION PLAN = owns campaign sequencing on g only
ORCHESTRATOR = supplies applicable cross-cutting execution/safety/verification/closure invariants
TOOLS/TESTS/SCANNERS = evidence only
HISTORICAL PLANS/FINDINGS = evidence/falsification probes only
```

Do not duplicate orchestrator laws into a second durable governance system. Reference and apply them where needed; this plan overrides only their generic sequencing behavior for this explicit `g` campaign.

## Governing principle

`PRESERVE REQUIRED TRUTH, NOT EXISTING STRUCTURE`.

The inherited tree is not sacred. Preserve only proven required product/system behavior, persisted data/integrity, externally required contracts, security guarantees, and genuinely canonical implementation value.

Every material artifact must re-earn its right to exist: file, directory, package, module, route, schema object, migration, contract, generated binding, runtime/config entry, script, workflow, test, fixture, mock, dependency, asset/manifest when material, and authoritative document.

If current shape is not justified, use the treatment proven by evidence:

`HARDEN | REFACTOR | REHOME | RENAME | MERGE | SPLIT | REWRITE | REGENERATE | MIGRATE | DELETE | BOUNDED_RETIREMENT`.

## The only allowed execution order

Do not begin by following bugs, historical root IDs, CI reds, scanner findings, compiler errors, or the easiest files.

Execute in this exact order:

```text
STEP 0 — PIN EXACT LIVE G + LOAD AUTHORITIES
↓
STEP 1 — COMPLETE BRANCH-WIDE STRUCTURAL CENSUS
↓
STEP 2 — BUILD CURRENT G ARCHITECTURE MODEL
↓
STEP 3 — DEFINE CANONICAL TARGET ARCHITECTURE
↓
STEP 4 — COMPUTE CURRENT→CANONICAL STRUCTURAL DELTA
↓
STEP 5 — SYNTHESIZE/RANK TRUE CAUSAL ROOTS
↓
STEP 6 — DECLARE FIRST CAUSALLY COMPLETE CLOSURE UNIT
↓
STEP 7 — RESTRUCTURE / MIGRATE / CUT OVER / DELETE / RENAME
↓
STEP 8 — VERIFY / FALSIFY / COMMIT / PUSH / RE-PIN
↓
STEP 9 — UPDATE CURRENT/CANONICAL/DELTA + RE-DIAGNOSE / RE-RANK
↓
REPEAT STEPS 6–9 UNTIL DYNAMIC ROOT GRAPH IS EMPTY
↓
STEP 10 — FRESH BRANCH-WIDE ADVERSARIAL RE-CENSUS
↓
STEP 11 — FINAL EXACT-SHA QUALIFICATION
↓
G_RADICAL_CLEANUP_COMPLETE
↓
FREEZE G / NO PR / NO MERGE / STOP
```

## Hard pre-mutation gate

Before Steps 0–5 are complete, material product/architecture mutation is forbidden.

```text
BUG_FIX_QUEUE=FORBIDDEN
CLEAN_AS_YOU_GO=FORBIDDEN
DELETE_AS_YOU_DISCOVER=FORBIDDEN
RENAME_AS_YOU_DISCOVER=FORBIDDEN
HISTORICAL_FINDING_AS_EXECUTION_ORDER=FORBIDDEN
READ_ONLY_DISCOVERY_AND_MODELING=REQUIRED
```

Plan-only corrections needed to keep this protocol coherent are allowed.

## Historical findings

Any prior labels such as serviceability, WLT, dispatch, checkout, cart, support, shared/core, infra/config, CI evidence, or similar are only **historical evidence/falsification probes**.

They gain execution status only after Steps 1–5 prove that they are still live independent causal roots. If they are descendants of a higher root, merge them under it. If stale, mark them superseded/false. Numeric/historical order has zero scheduling authority.

## Success meaning

The goal is not "tests green" and not "all old findings checked".

The goal is a branch whose material structure is the best proven canonical state:

`CLEAR OWNERSHIP + UNIQUE WRITERS + CORRECT BOUNDARIES + TRUTHFUL NAMES/PATHS + COMPLETE MIGRATIONS + NO PARALLEL/SHADOW TRUTH + NO DEAD/STALE/OBSOLETE RESIDUE + NO PATCH-SHAPED ARCHITECTURE + ZERO KNOWN MATERIAL NOISE`.
