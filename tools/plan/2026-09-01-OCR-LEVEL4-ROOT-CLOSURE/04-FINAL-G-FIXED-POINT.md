# FINAL `g` FIXED-POINT VERIFICATION

## 0. Scope

This file qualifies **branch `g` only**.

It does not evaluate, compare, merge, rebase, synchronize, or prepare any other branch.

`GREEN != CLOSED`.

Verification must prove not merely that behavior works, but that the final material structure of `g` is the best proven canonical structure justified by current product/system truth and applicable engineering best practices.

Final qualification therefore verifies both:

`REQUIRED TRUTH PRESERVED`
+
`DEFECTIVE HISTORICAL STRUCTURE REMOVED OR REBUILT`.

## 1. Canonical reconstruction success definition

The campaign succeeds only when the exact final `g` candidate proves:

```text
REQUIRED PRODUCT/SYSTEM TRUTH PRESERVED
REQUIRED DATA/INTEGRITY PRESERVED
WRONG STRUCTURE REBUILT
CANONICAL OWNERSHIP ESTABLISHED
CANONICAL WRITERS UNIQUE
FILENAMES TRUTHFUL
DIRECTORY NAMES TRUTHFUL
PACKAGE/MODULE NAMES TRUTHFUL
DIRECTORY/PACKAGE PATHS CANONICAL
BOUNDARIES MATCH ACTUAL RESPONSIBILITIES
LANGUAGE/FRAMEWORK CONVENTIONS RESPECTED WHERE MATERIAL
DUPLICATE AUTHORITIES REMOVED
SUPERSEDED STRUCTURE DELETED
OLD PATHS/NAMES REMOVED
DEAD TOOLING REMOVED
UNUSED DEPENDENCIES REMOVED
NO MATERIAL PATCH-SHAPED ARCHITECTURE
NO MATERIAL TRANSITIONAL TOPOLOGY LEFT AS PERMANENT DESIGN
ZERO KNOWN MATERIAL RESIDUE
```

The goal is not to preserve inherited structure. The goal is:

`PRESERVE REQUIRED TRUTH, NOT EXISTING STRUCTURE`.

## 2. Every-artifact existence gate

At final branch-wide qualification, every material artifact must have re-earned its right to exist.

For each material file/directory/package/script/workflow/config/route/dependency/test/fixture/mock/generated repair/internal API/wrapper/adapter/schema object/runtime entry/authoritative document, final evidence must establish a valid reason such as:

`CANONICAL_REQUIRED`
`DERIVED_REQUIRED`
`GENERATED_REQUIRED`
`CURRENT_RUNTIME_OR_TOOLING_CONSUMER`
`SUPPORTED_BOUNDED_MIGRATION`
`REQUIRED_EVIDENCE`.

If no valid reason exists, final qualification fails until the artifact is removed.

No artifact may survive solely because it is old, harmless, familiar, historically useful, costly to migrate, or potentially useful later.

## 3. Evidence ladder

Use where applicable:

1. static source/ownership proof;
2. focused unit/component tests;
3. schema/contract/generated consistency;
4. migration/backfill/reconciliation/readback;
5. runtime/integration tests;
6. product journey/device/browser evidence;
7. CI/security/scanner evidence;
8. naming/path/topology and ownership review;
9. structural negative-space audit;
10. exact-candidate branch-wide qualification;
11. fresh adversarial audit of all material content on `g`.

Evidence from a different SHA or different branch is context only unless independently revalidated for the exact `g` candidate.

## 4. Evidence-debt law

Every material test/CI/scanner/runtime/review/device finding must be:

`PROVENANCE → VALIDATE/FALSIFY → DEDUPLICATE → MAP TO ROOT/FALSE POSITIVE → ACT → CONSUME`.

No orphan finding and no success-only interpretation.

## 5. Per-root verification

Before closure verify dimensions invalidated by the root, including where applicable:

- canonical semantics/owner/writer uniqueness;
- canonical target name/path/topology;
- language/framework/ecosystem convention where materially relevant;
- schema/constraints/clean install;
- existing-data migration/backfill/readback;
- contract/generated parity;
- API/runtime behavior;
- all affected consumers;
- scripts/workflows/config/tooling invocation paths;
- product journeys;
- auth/security/privacy;
- concurrency/idempotency/recovery;
- A11y/RTL/LTR;
- deletion/reference absence.

A behaviorally correct implementation with misleading names, misplaced files, patch-shaped topology, duplicate boundaries, or obsolete paths is not structurally closed.

## 6. Independent falsification

Attempt to disprove closure by searching for:

- second writers/owners;
- misleading filenames/directories/packages;
- paths that imply the wrong canonical owner;
- duplicate responsibility trees;
- old path/import/route/config/env names;
- old script/workflow/tool invocations;
- manual mappings/wrappers/contract repairs;
- stale fixtures/tests/mocks/snapshots;
- dead files/packages;
- legacy/history/archive/backup/copy/temp residue;
- `old/new/v2/v3/fixed/final` style transitional names used as permanent architecture without real domain meaning;
- error/missing/unknown converted to plausible success;
- clean-install vs upgraded-state divergence;
- unused dependencies/plugins;
- generic `utils/helpers/common/misc/shared/core` buckets carrying responsibilities that belong to a more precise owner;
- framework/language structure materially diverging from established conventions without a justified repository-specific reason;
- patch/workaround layers that survive despite the underlying root having been corrected.

Closure requires the falsification attempt to find no material contradiction.

## 7. Structural negative-space gate

Before root closure prove as applicable:

```text
OLD_WRITERS=0
OLD_READERS=0
OLD_IMPORTS=0
OLD_REEXPORTS=0
OLD_ROUTES=0
OLD_CONFIG_REFS=0
OLD_SCRIPT_WORKFLOW_REFS=0
OLD_RUNTIME_REACHABILITY=0
OLD_NAME_PATH_REFS=0
UNMIGRATED_CONSUMERS=0
UNEXPECTED_GENERATED_DIFF=0
UNBOUNDED_COMPATIBILITY=0
THIRD_AUTHORITY=0
EXPECTED_DELETIONS_MISSING=0
UNDISPOSITIONED_MATERIAL_ARTIFACTS=0
MISLEADING_MATERIAL_FILENAMES=0
MISLEADING_MATERIAL_DIRECTORY_NAMES=0
MISPLACED_MATERIAL_ARTIFACTS=0
DUPLICATE_MATERIAL_RESPONSIBILITY_TREES=0
UNBOUNDED_OLD_PATH_ALIASES=0
KNOWN_NONCANONICAL_MATERIAL_TOPOLOGY=0
```

Functional pass with residue or structural/naming debt is not closure.

## 8. Best-practice material conformance gate

Final structure must be `BEST-PRACTICE CONFORMANT WHERE MATERIAL AND APPLICABLE`.

This is not a style contest. Verify material engineering outcomes:

```text
CLEAR_OWNERSHIP=PASS
LOW_UNJUSTIFIED_COUPLING=PASS
HIGH_COHESION=PASS
PREDICTABLE_PATHS=PASS
FRAMEWORK_NATIVE_STRUCTURE=PASS_OR_JUSTIFIED_EXCEPTION
NO_CIRCULAR_OWNERSHIP=PASS
EXPLICIT_CONTRACTS=PASS
GENERATED_CODE_DERIVED_FROM_CANONICAL_SOURCE=PASS
MINIMUM_NECESSARY_LAYERS=PASS
CLEAR_RUNTIME_COMPOSITION=PASS
TESTABILITY=PASS
OBSERVABILITY=PASS_OR_JUSTIFIED_NA
SECURITY=PASS
DATA_INTEGRITY=PASS
MAINTAINABILITY=PASS
```

A convention is not mandatory merely because it is fashionable. But a materially non-standard structure that harms ownership clarity, tooling, runtime correctness, maintainability, security, or integrity must be corrected or explicitly justified by stronger repository-specific evidence.

## 9. Queue exhaustion is not closure

When the known Root Graph first becomes empty, run a fresh independent audit across **all material content on `g`**, not only files/directories already mentioned in the plan.

Cover:

`product/system behavior`, `code/architecture`, `names/paths/topology`, `language/framework conventions`, `data/schema/migrations/clean-install`, `contracts/generated`, `runtime/config/env`, `scripts/tooling/CLI`, `workflows/actions`, `security/auth/privacy`, `concurrency/recovery/idempotency`, `all application journeys/UX/A11y/RTL`, `filesystem ownership`, `semantic duplicates`, `parallel/shadow truth`, `dead/stale/obsolete/legacy/temp residue`, `compatibility`, `dependencies`, `exports/routes/config`, `tests/fixtures/mocks/snapshots`, `documents/manifests with live authority`.

Any material finding reopens the graph.

## 10. Final branch-wide canonical reconstruction sweep

Run on the exact final `g` candidate:

`semantic duplicate scan → mutable writer uniqueness → owner uniqueness → every-artifact right-to-exist proof → canonical naming/path/topology → contract authority → schema/data authority → generated/manual repair → state-machine/policy/default/mapping duplication → filesystem responsibility → script/tool/workflow uniqueness → framework/language material convention audit → dead/unreachable → legacy/history/archive/backup/copy/temp → compatibility residue → unused dependency/export/config/route/script/workflow → test/fixture/mock residue → reachable old authority → patch-shaped architecture scan → undispositioned artifact scan`.

Every material hit must have a proven disposition.

## 11. Exact final candidate qualification

Pin one immutable `g` SHA and require:

```text
KNOWN_MATERIAL_ROOTS=0
KNOWN_MATERIAL_GAPS=0
KNOWN_MATERIAL_UNKNOWNS=0
UNDISPOSITIONED_FINDINGS=0
EVIDENCE_DEBT=0
UNMIGRATED_CONSUMERS=0
REACHABLE_OLD_AUTHORITIES=0
PARALLEL_TRUTHS=0
SHADOW_AUTHORITIES=0
DUPLICATE_MUTABLE_WRITERS=0
UNBOUNDED_COMPATIBILITY=0
MATERIAL_DEAD_STALE_OBSOLETE_RESIDUE=0
MATERIAL_UNUSED_SCRIPT_WORKFLOW_TOOLING=0
MATERIAL_UNUSED_DEPENDENCY_CONFIG_ROUTE=0
MATERIAL_FILESYSTEM_NOISE=0
MISLEADING_FILENAMES=0
MISLEADING_DIRECTORY_NAMES=0
MISPLACED_FILES=0
AMBIGUOUS_DIRECTORIES=0
DUPLICATE_RESPONSIBILITY_TREES=0
UNBOUNDED_COMPAT_PATHS=0
UNOWNED_PACKAGES=0
DEAD_SCRIPTS=0
UNUSED_DEPENDENCIES=0
KNOWN_MATERIAL_PATCH_SHAPED_ARCHITECTURE=0
KNOWN_NONCANONICAL_MATERIAL_TOPOLOGY=0
CLEANUP_OBLIGATIONS=0
MATERIAL_REGRESSIONS=0
FRESH_BRANCH_WIDE_ADVERSARIAL_REAUDIT=PASS
BEST_PRACTICE_MATERIAL_CONFORMANCE=PASS
NEGATIVE_SPACE=PASS
```

If any field is unknown, final qualification fails.

## 12. Final output

Only when the exact `g` candidate passes:

```text
G_RADICAL_CLEANUP_COMPLETE
EXACT_G_SHA=<immutable sha>
LEVEL_4_EVIDENCE_STATE=PASS
ROOT_CORRECTNESS_STATE=PASS
STRUCTURAL_RECONSTRUCTION_STATE=PASS
CANONICAL_UNIQUENESS_STATE=PASS
CANONICAL_NAMING_PATH_TOPOLOGY_STATE=PASS
BEST_PRACTICE_MATERIAL_CONFORMANCE_STATE=PASS
ZERO_PARALLEL_TRUTH_STATE=PASS
ZERO_RESIDUE_STATE=PASS
G_NOISE_BUDGET=ZERO_KNOWN_MATERIAL_NOISE
KNOWN_REMAINING_ROOTS=0
KNOWN_MATERIAL_GAPS=0
KNOWN_MATERIAL_UNKNOWNS=0
```

Do not emit `MERGE_READY` or `G_LEVEL4_CANDIDATE_READY`.

## 13. Freeze / stop boundary

After final output:

```text
FREEZE EXACT G SHA
DO NOT OPEN PR
DO NOT MERGE
DO NOT REBASE OR AUTO-SYNC
KEEP g ISOLATED
STOP ACTIVE CLEANUP CAMPAIGN
```

Any later decision involving another branch is a new, separately authorized campaign and is not implied by this plan.
