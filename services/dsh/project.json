{
  "name": "dsh",
  "root": "services/dsh",
  "sourceRoot": "services/dsh",
  "projectType": "application",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/run-tsc-check.mjs --project services/dsh/tsconfig.json --label dsh"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir services/dsh build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/run-command-check.mjs --label dsh-lint -- pnpm --dir services/dsh lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node tools/scripts/run-command-check.mjs --label dsh-build -- pnpm --dir services/dsh build",
          "node tools/scripts/run-command-check.mjs --label dsh-contracts -- node --test services/dsh/tests/*.test.mjs"
        ],
        "parallel": false
      }
    }
  },
  "tags": [
    "scope:dsh",
    "type:service"
  ]
}
