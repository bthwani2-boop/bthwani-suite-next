# BThwani DSH OpenAPI schemas module: marketing-commercial.
# Source of truth. Referenced by ../dsh.openapi.yaml; do not add an openapi root here.

DshMarketingTargetType:
  type: string
  enum: [home, stores, store, category, subcategory, product, offer, campaign, search, custom]

DshMarketingAudience:
  type: string
  enum: [all, client, partner, captain, field]

DshCampaignPlacement:
  type: string
  enum: [home, hero, feed, floating, banner, store-card]

DshCampaign:
  type: object
  additionalProperties: false
  required: [id, title, description, status, startDate, endDate, audience, version, createdBy, createdAt, updatedAt]
  properties:
    id: { type: string }
    title: { type: string }
    description: { type: string }
    status: { type: string, enum: [draft, active, paused, completed, cancelled] }
    startDate: { type: string, format: date }
    endDate: { type: string, format: date }
    targetType: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshMarketingTargetType" }
    targetId: { type: string }
    audience: { $ref: "#/DshMarketingAudience" }
    placement: { $ref: "#/DshCampaignPlacement" }
    version: { type: integer, minimum: 1 }
    createdBy: { type: string }
    archivedAt: { type: ["string", "null"], format: date-time }
    createdAt: { type: string, format: date-time }
    updatedAt: { type: string, format: date-time }

DshCampaignsListResponse:
  type: object
  additionalProperties: false
  required: [campaigns]
  properties:
    campaigns:
      type: array
      items: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshCampaign" }

DshCampaignResponse:
  type: object
  additionalProperties: false
  required: [campaign]
  properties:
    campaign: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshCampaign" }

DshCreateCampaignRequest:
  type: object
  additionalProperties: false
  required: [title, startDate, endDate]
  properties:
    title: { type: string, minLength: 1 }
    description: { type: string }
    startDate: { type: string, format: date }
    endDate: { type: string, format: date }
    targetType: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshMarketingTargetType" }
    targetId: { type: string }
    audience: { $ref: "#/DshMarketingAudience" }
    placement: { $ref: "#/DshCampaignPlacement" }

DshUpdateCampaignRequest:
  type: object
  additionalProperties: false
  required: [expectedVersion]
  properties:
    expectedVersion: { type: integer, minimum: 1 }
    title: { type: string, minLength: 1 }
    description: { type: string }
    status: { type: string, enum: [draft, active, paused, completed, cancelled] }
    startDate: { type: string, format: date }
    endDate: { type: string, format: date }
    targetType: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshMarketingTargetType" }
    targetId: { type: string }
    audience: { $ref: "#/DshMarketingAudience" }
    placement: { $ref: "#/DshCampaignPlacement" }

DshPartnerOffer:
  type: object
  additionalProperties: false
  required: [id, title, partnerName, storeId, storeLabel, productId, productLabel, category, offerType, status, source, valueLabel, eligibility, version, createdBy, createdBySurface, createdAt, updatedAt]
  properties:
    id: { type: string }
    title: { type: string }
    partnerName: { type: string }
    storeId: { type: string }
    storeLabel: { type: string }
    productId: { type: string }
    productLabel: { type: string }
    category: { type: string }
    offerType: { type: string, enum: [discount, free-delivery, bundle, buy-x-get-y, coupon] }
    couponId: { type: string }
    status: { type: string, enum: [inbound, review, marketing-ready, published, paused, rejected, archived] }
    source: { type: string, enum: [partner, control-panel] }
    valueLabel: { type: string }
    eligibility: { type: string, enum: [all, client] }
    activeFromDate: { type: string, format: date }
    activeToDate: { type: string, format: date }
    rejectionReason: { type: string }
    marginRiskNote: { type: string }
    version: { type: integer, minimum: 1 }
    linkedCampaignId: { type: string }
    createdBy: { type: string }
    createdBySurface: { type: string }
    createdAt: { type: string, format: date-time }
    updatedAt: { type: string, format: date-time }

DshPartnerOffersListResponse:
  type: object
  additionalProperties: false
  required: [offers]
  properties:
    offers:
      type: array
      items: { $ref: "#/DshPartnerOffer" }

DshPartnerOfferResponse:
  type: object
  additionalProperties: false
  required: [offer]
  properties:
    offer: { $ref: "#/DshPartnerOffer" }

DshUpdatePartnerOfferRequest:
  type: object
  additionalProperties: false
  required: [expectedVersion]
  properties:
    expectedVersion: { type: integer, minimum: 1 }
    status: { type: string, enum: [inbound, review, marketing-ready, published, paused, rejected] }
    title: { type: string, minLength: 1 }
    valueLabel: { type: string, minLength: 1 }
    eligibility: { type: string, enum: [all, client] }
    activeFromDate: { type: string, format: date }
    activeToDate: { type: string, format: date }
    rejectionReason: { type: string }
    marginRiskNote: { type: string }
    couponId: { type: string }

DshSubmitPartnerOfferRequest:
  type: object
  additionalProperties: false
  required: [title, valueLabel]
  properties:
    title: { type: string, minLength: 1 }
    partnerName: { type: string }
    storeLabel: { type: string }
    productId: { type: string }
    productLabel: { type: string }
    category: { type: string }
    offerType: { type: string, enum: [discount, free-delivery, bundle, buy-x-get-y, coupon] }
    valueLabel: { type: string, minLength: 1 }
    eligibility: { type: string, enum: [all, client] }

DshMarketingTicker:
  type: object
  required: [id, message, kind, status, source, audience, deliveryMode, priority, pinned, actionType, actionTarget, clicks, impressions, createdBy, createdAt, updatedAt]
  properties:
    id: { type: string }
    message: { type: string }
    kind: { type: string, enum: [alert, news, promo] }
    status: { type: string, enum: [draft, published, paused] }
    source: { type: string, enum: [system, ops, partner] }
    audience: { type: string, enum: [all, client, partner, captain] }
    deliveryMode: { type: string, enum: [scroll, toast, overlay] }
    priority: { type: string, enum: [low, normal, high, critical] }
    pinned: { type: boolean }
    actionType: { type: string }
    actionTarget: { type: string }
    clicks: { type: integer, minimum: 0 }
    impressions: { type: integer, minimum: 0 }
    openHour: { type: integer, minimum: 0, maximum: 23 }
    closeHour: { type: integer, minimum: 0, maximum: 23 }
    cooldownMinutes: { type: integer, minimum: 0 }
    repeatGapMinutes: { type: integer, minimum: 0 }
    createdBy: { type: string }
    createdAt: { type: string }
    updatedAt: { type: string }

DshMarketingTickersListResponse:
  type: object
  required: [tickers]
  properties:
    tickers:
      type: array
      items: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshMarketingTicker" }

DshMarketingTickerResponse:
  type: object
  required: [ticker]
  properties:
    ticker: { $ref: "../../dsh.openapi.yaml#/components/schemas/DshMarketingTicker" }

DshCreateMarketingTickerRequest:
  type: object
  required: [message]
  properties:
    message: { type: string, minLength: 1 }
    kind: { type: string, enum: [alert, news, promo] }
    status: { type: string, enum: [draft, published, paused] }
    source: { type: string, enum: [system, ops, partner] }
    audience: { type: string, enum: [all, client, partner, captain] }
    deliveryMode: { type: string, enum: [scroll, toast, overlay] }
    priority: { type: string, enum: [low, normal, high, critical] }
    pinned: { type: boolean }
    actionType: { type: string }
    actionTarget: { type: string }
    openHour: { type: integer, minimum: 0, maximum: 23 }
    closeHour: { type: integer, minimum: 0, maximum: 23 }
    cooldownMinutes: { type: integer, minimum: 0 }
    repeatGapMinutes: { type: integer, minimum: 0 }

DshUpdateMarketingTickerRequest:
  type: object
  properties:
    message: { type: string, minLength: 1 }
    kind: { type: string, enum: [alert, news, promo] }
    status: { type: string, enum: [draft, published, paused] }
    source: { type: string, enum: [system, ops, partner] }
    audience: { type: string, enum: [all, client, partner, captain] }
    deliveryMode: { type: string, enum: [scroll, toast, overlay] }
    priority: { type: string, enum: [low, normal, high, critical] }
    pinned: { type: boolean }
    actionType: { type: string }
    actionTarget: { type: string }
    openHour: { type: integer, minimum: 0, maximum: 23 }
    closeHour: { type: integer, minimum: 0, maximum: 23 }
    cooldownMinutes: { type: integer, minimum: 0 }
    repeatGapMinutes: { type: integer, minimum: 0 }
