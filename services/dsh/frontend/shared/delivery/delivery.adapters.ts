import { useCaptainOrderRuntime } from './use-captain-order-runtime';

/**
 * Hook to retrieve delivery order runtime (adapts captain order runtime).
 */
export const useDeliveryOrderRuntime = useCaptainOrderRuntime;
