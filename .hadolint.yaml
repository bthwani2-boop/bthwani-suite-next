# .hadolint.yaml
# Hadolint Dockerfile linter rules configuration.
#
# No repository-wide rule suppressions are permitted here. A genuinely
# intentional exception belongs on the narrow owning Dockerfile with its
# evidence and removal condition.
