# Closure contract — Control Panel all sections and directly related surfaces

Current decision: **NEEDS_EVIDENCE**. This package being `READY` means the diagnosis and implementation map are complete enough to execute; it does not mean the product is closed.

Final closure is allowed only on one immutable candidate SHA after U001–U008 are `DONE` and their required checks are recorded against that same candidate. Every current Control Panel section, hidden/detail route, root bootstrap, shell, BFF/server boundary and directly proven sovereign owner must either be verified or have a specific evidence-backed exclusion. No unresolved blocker, unknown-result financial mutation, stale migration conflict, cross-scope access path, parallel truth owner, mock/fallback production truth, optimistic success without canonical readback, or duplicate obsolete implementation may remain.

The final gate must include: strict package validation; Control Panel build/typecheck/test/lint; applicable Identity, Workforce, Platform Control, Providers, DSH and WLT tests/build/typechecks; OpenAPI/generated-client and full-stack/API-binding drift gates; database migration/schema/contract tests where persistence changed; trusted-context and object/business-scope negative tests; conflict/idempotency/concurrency/response-loss cases; WLT financial-boundary and reconciliation evidence; and runtime smoke for the exact candidate.

Where applicable Product Truth requires runtime evidence, visual evidence, security/privacy review, finance-control review, product-owner approval, QA/device/accessibility review, or release approval, those approvals are external evidence and cannot be self-granted by implementation. Their absence blocks the corresponding closure decision even if code checks pass.

Visual closure must prove Arabic RTL, keyboard/focus behavior, screen-reader semantics, large-text resilience, loading/empty/error/forbidden/stale/conflict/unknown-result states, and no deceptive success/health/readiness display. Cross-surface closure is restricted to direct consequences of Control Panel operations; unrelated surface-only functionality remains outside this package.

If `BB` moves after implementation begins, re-resolve the branch, classify the delta as disjoint/related/overlap/conflict/authority change, reconcile related changes, re-run affected verification, and re-pin the final candidate before any `CLOSED_WITH_EVIDENCE` decision.
