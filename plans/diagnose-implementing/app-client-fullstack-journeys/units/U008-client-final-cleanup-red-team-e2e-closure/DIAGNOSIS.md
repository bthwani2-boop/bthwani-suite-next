# U008 diagnosis — visible-action census, cleanup, red-team and exact-SHA closure

The prior terminal U007 was necessary but incomplete. It focused on generic cleanup and exact-SHA checks while the independent audit found a concrete visible-but-unreachable profile action and a likely legacy wallet alias. A final closure gate that does not enumerate every user-visible action, deep link, route, adapter and exported compatibility path can report green unit tests while the shipped client still contains inert business functionality or dead residue. The terminal unit is therefore moved to U008 and strengthened with a mandatory reachability/reference census.

Current CI also changed materially. The former OpenAPI postinstall circular-reference and Identity readiness findings are superseded: current Node workspace setup succeeds and Identity backend/database is green. The current DSH full suite fails on a Partner-evidence readiness migration mismatch; WLT fails its readiness check plus unrelated payout test compilation; project-wide journey bindings fail on Field payout/onboarding; runtime full smoke fails on Partner payout-destination HTTP 405; an unrelated Control Panel field-assignment style gate also fails. These failures are real but do not authorize app-client work. U008 must classify each final failure by an actual client trace, never by the desire to make all repository jobs green from this package.

U008 depends on U001-U007. It removes only proven client-scope dead/duplicate/legacy residue, attacks cross-unit actor/version/replay/restart/provider/payment/subscription/privacy races, then re-pins after the last write and executes strict package, build, database, runtime, native/device and CI evidence on the exact final SHA. Any client finding reopens its owner unit. Any unrelated failure remains visible as an external blocker for its owner and cannot be hidden, waived or imported into the client implementation.
