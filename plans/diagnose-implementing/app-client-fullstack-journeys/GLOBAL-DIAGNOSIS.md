# Global diagnosis — app-client fail-closed rebaseline

## Rebaseline and concurrent movement

The previous client package was authored at `c14c208bbe778c6310123945d53820317f2b3798`. Deep diagnosis ran against `f48d27e09e17dffaa471f394a46cd2878d3c1d86`, where the branch was already 308 commits ahead and had materially changed app-client runtime/tests, mobile transport, Identity, DSH catalog/cart/checkout/orders/support, WLT and verification infrastructure. Immediately before write, `BB` advanced one more commit to `d6ff3a2463e4a6d748bbe6ef8955281e4ef25c28`. Comparison proved that commit touches only `plans/diagnose-implementing/app-partner-fullstack-journeys`; no client package/product/runtime/dependency path changed. It was classified DISJOINT, preserved, and the final client package was re-pinned/rebuilt above it. The final pin is therefore 309 commits ahead of the former client package commit.

## Current implementation versus remaining root defects

Identity now implements refresh single-flight/rotation conflict handling and contains migration `identity-022_refresh_rotation_concurrency.sql`; app-client clears persisted query cache and offline mutation queue before session end. Refresh concurrency is verification evidence, not implementation work to repeat. Remaining closure is exact lifecycle isolation across restore, refresh, revoke, logout, wrong role/surface, actor replacement, restart/offline and push registration. Actions evidence finds a current readiness defect: Identity runtime still expects migration 021 while the governed manifest requires 022. DSH also fails `TestRequireActorRejectsSystemSurfaceForMobileRole` because authenticated-forbidden mobile/system-surface behavior returns 401 instead of the required 403; U001 owns both corrections without weakening authentication.

DSH now has canonical client catalog projection plus assortment/publication runtime truth. U002 no longer asks for another projection; it closes publication/store/assortment/inventory/pricing/media → canonical projection → client runtime convergence and special-request idempotency/serviceability. Support likewise converged to the actor-generic `/dsh/support/tickets` API, so U006 protects that stronger owner-scoped design and forbids resurrection of retired client-specific aliases.

The strongest confirmed client implementation defect is checkout OCC. `ComputeCheckoutSnapshotTx` locks the cart and validates `expectedVersion`; `ComputeCheckoutSnapshotForClientTx` is explicitly the compatibility path for callers that do not carry expected version. Current HTTP checkout still calls that compatibility wrapper. U003 must propagate expected cart version through OpenAPI, generated binding, frontend controller/request and HTTP handler; stale cart must conflict before order creation, and the compatibility bypass must be removed once no legitimate caller remains. UI refresh/silent retry is not an acceptable substitute. Exactly-once order creation, immutable snapshots, canonical serviceability and WLT financial ownership remain mandatory.

WLT remains the sole client financial truth owner. Current Actions evidence shows runtime readiness requiring `wlt-930_checkout_quote_payment_session_guard.sql` while the governed manifest requires `wlt-931_settlement_batch_mutation_idempotency.sql`; U005 must converge readiness plus own-wallet/payment/refund/reconciliation behavior. The same WLT artifact has payout-governance test compile failures, but customer payout/withdrawal is not authorized by current Product Truth. That defect is documented as outside implementation scope and may block broad verification without authorizing unrelated payout work in this package.

## Verification infrastructure and final closure

Most Node/policy/runtime jobs in Actions fail at the same `Set up locked Node workspace` root: `pnpm install --frozen-lockfile` reaches root postinstall, then generated OpenAPI materialization fails inside Redocly with `Self-referencing circular pointer`. The artifact does not identify the registry context, so U007 must isolate it deterministically. If a client-consumed Identity/DSH/WLT contract owns the cycle, repair its canonical contract/composition source; otherwise keep it external while still blocking final closure until green.

U007 is a new unit only inside this existing package. It runs final cleanup, removes proven dead/duplicate/retired compatibility residue, attacks wrong actor/role/surface, stale version, duplicate/replay, concurrency, timeout, unknown external outcome, offline/restart, stale cache/push, unauthorized deep-link/location/proof and cross-surface races, then runs strict package validation and applicable exact-SHA build/database/runtime/native/visual/security/finance/CI gates. Any failure reopens the responsible unit. No unit is DONE from source inspection, and `CLOSED_WITH_EVIDENCE` remains forbidden until the exact final candidate has zero known actionable client-scope defects and all required protected evidence is present.
