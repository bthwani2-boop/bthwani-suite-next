# App Field final closure — START HERE

Repository: `bthwani2-boop/bthwani-suite-next`  
Target branch: `BB`  
Diagnosis pin: `397dcae545d723d88e96828535973533f4f6ad68`  
Mode: `RESUME_AND_RECONCILE`

This is the existing `app-field-final-closure` package re-diagnosed against current `BB`; it is not a new package. Functional scope is restricted to app-field and only the exact Identity, Workforce, DSH, WLT, Media, control-panel, partner and client seams causally required by field-originated or field-required journeys. `app-captain` remains structurally represented for validator coverage but is functionally excluded under current Product Truth.

Historical `RESULT.json` files are preserved for provenance only. A historical `PASS` does not close current `BB` unless the required checks are repeated on the current implementation candidate and recorded against that exact SHA. Execute U001 → U002 → U003/U004 → U005 → U006/U007/U008 → U009. Start each unit by reproducing/verifying current behavior; change product code only for a reproduced defect at the authoritative owner.

Root rule: repair the owning contract/state machine/test gate once. Do not add UI-only fallbacks, duplicate local truth, alternate endpoints, shadow queues, static-grep proof, or package-only changes that hide a product defect.

Before final closure: re-resolve `BB`, reconcile concurrent movement, run all required candidate-bound checks, perform real-device field QA and same-store cross-surface readback, then update results. `CLOSURE.md` remains `NOT_CLOSED` until every required gate passes.
