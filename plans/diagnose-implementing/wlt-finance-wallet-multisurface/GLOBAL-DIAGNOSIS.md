# Global Diagnosis — WLT Finance / Wallet Multi-Surface

## Candidate
Re-diagnosed from the `BB` package baseline and updated for the canonical financial-authority decision. All prior package evidence remains candidate-bound provenance only; final closure must re-pin the newest `BB` head after the last finance-related write and prove the complete same-candidate path. Concurrent DSH/WLT changes must be classified and reconciled before closure rather than overwritten or assumed correct.

## Authority and boundary
Canonical authority is `wlt-money-movement-settlement.product-truth.json`, `representative-wallets.product-truth.json`, `settlements-commissions.product-truth.json`, `services/wlt/WLT_EXTERNAL_WALLET_SWITCH_ARCHITECTURE.md`, then current WLT/DSH contracts, code, migrations and runtime wiring. In scope is only proven finance/wallet ownership, money movement, accounting, financial readback, financial policy/evidence and the runtime/integration wiring required to prove it. Unrelated catalog/HR/maps/media/marketing/identity feature work is excluded unless a direct finance dependency is proven.

## Canonical financial-authority decision
All authoritative monetary values must be derived automatically by WLT from trusted operational events, canonical ledger state and versioned policy. Partner, captain and field surfaces are read-only for official-wallet destination master data and may never create, update, deactivate, replace or select that destination. Their payout intent is limited to `FULL_AVAILABLE` or `SPECIFIED` plus a specified amount only for the latter; WLT resolves current eligible funds and the current `VERIFIED + ACTIVE_FOR_PAYOUT` destination inside the authoritative server transaction and pins that destination version to the payout. Finance staff operate from server-calculated balances, liabilities, commissions, eligibility, control totals and variances; they do not establish authoritative financial truth through manual arithmetic or generic amount override fields. Official-wallet destination provisioning/change is a WLT-owned Finance master-data workflow requiring authorization, reason, evidence, versioning, verification and required independent approval before activation.

## Confirmed root-cause gaps
1. Package baseline/evidence was stale and U004/U005 were never executed. U003 also recorded a required failed check while declaring PASS, so prior closure posture was not fail-closed.
2. Canonical double-entry writes (`wlt_ledger_transactions/lines/accounts`) coexist with legacy `wlt_ledger_entries` and direct `wlt_wallets` balance mutation. Current read/control paths can therefore diverge from canonical accounting.
3. `settlement/daily_close.go` totals legacy ledger entries and computes unverified manual evidence but proceeds instead of blocking. Close is neither canonical nor fail-closed.
4. `payout/canonical_payout_destination.go` still defaults to bank and permits bank/mobile-money/manual semantics, conflicting with verified official-wallet destination truth. Manual is execution method, not beneficiary destination type.
5. Historical U002 evidence found the WLT API runtime wiring a nil wallet DecisionService, causing mutation kill-switch failure before provider flows. This is a package-level runtime blocker, not an acceptable environment waiver.
6. `penalty/penalty.go` posts canonical ledger transactions and also directly mutates `wlt_wallets`; its HTTP boundary accepts operator-context header input. Penalty was previously unassigned in the package.
7. `commercial/onboarding_fee.go` uses `float64`, a singleton `id=1` policy without visible operator-context scoping, and lacks durable audit. Subscription lifecycle/payment/compensation is financially material and was omitted.
8. `promotionfunding` accepts operatorContextId through payload/header and records monetary funding transitions without proving one canonical ledger effect. `pricing/quote.go` accepts price/fee monetary inputs; each must be governed by trusted source ownership, exact minor units, bounds and PaymentAllocation binding.
9. `dispatchfinancialeligibility` is a material WLT financial decision based on captain wallet/policy; it must consume the same canonical wallet truth and DSH must honor context/policy/TTL without recalculating finance.
10. Product Truth requires current-candidate readback on app-client, app-partner, app-captain, app-field and control-panel. Static module/screen presence is not runtime, authorization, offline or visual evidence.
11. Beneficiary-facing payout destination mutation is an architectural financial-control defect, not a UI defect. Any partner/captain/field self-service create/update/deactivate/replace destination endpoint, controller, shared component or form must be removed or denied server-side. Likewise, any beneficiary-selected `destinationId`, provider, beneficiary name or wallet identifier in payout intent is forbidden. `FULL_AVAILABLE` must be calculated transactionally by WLT at request time; `SPECIFIED` must be validated against the same live eligible balance. Finance destination changes must follow governed maker/checker-style master-data controls, and Finance must never directly type or edit authoritative balances, commissions, payable totals or settlement totals.

## Corrected execution ownership
- U004: official-wallet destination master-data ownership; removal of beneficiary self-service mutation; Finance-only reason/evidence/version/verification/approval lifecycle; server-resolved payout destination; `FULL_AVAILABLE`/`SPECIFIED` payout intent; payout holds/approval; immutable manual settlement; evidence; reconciliation; canonical daily close.
- U006: canonical ledger/wallet convergence, direct writer retirement, penalty and canonical representative ledger/wallet read model.
- U007: commercial/subscription/onboarding fee, promotion funding and pricing/PaymentAllocation controls, with all authoritative values derived from trusted events/policy and no generic manual monetary override.
- U008: runtime financial DecisionService/kill-switch, provider-mode/unknown-result readiness, dispatch financial eligibility and DSH retry/outbox integration.
- U005: final cross-surface finance control/readback after U004/U006/U007/U008; prove read-only beneficiary destination UI, amount-mode-only payout intent, automatic financial effects/readback, finance control-panel no-manual-arithmetic behavior, authorization negatives and same-SHA end-to-end closure.

## Fail-closed rule
Remain OPEN until one latest candidate proves: single-source accounting; zero unauthorized direct balance writers; zero beneficiary payout-destination mutation paths; zero client-selected payout destination/provider/beneficiary identifiers; zero generic manual authoritative monetary override paths; trusted operator-context isolation; idempotent/correlation-bound mutations; automatic event/policy-derived monetary effects; transactional `FULL_AVAILABLE` and bounded `SPECIFIED` payout resolution; Finance-only governed official-wallet destination lifecycle; official-wallet/manual-settlement evidence; ambiguous-result reconciliation; blocking DailyFinanceClose; exact commercial/promotion/pricing money policy; current contracts/generated clients; executable governed runtime financial mutations; dispatch eligibility freshness; DSH retry exactly-once behavior; cross-actor/context negatives; and all five required surfaces. Historical PASS, cached tests, partial pass or environment excuses do not close finance.
