# Master Reconstruction Command — Whole BThwani System

Use this as the single operator command for this package.

---

## Invocation

`BRANCH: b`

`TARGET: كل الأسطح والخدمات وكل النظام End-to-End`

`MODE: PREPARE_ONLY` for the current package-preparation pass.

Use exclusively the current V5 executable orchestration semantics defined by:

`tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`

together with the current V5 package schema/engine/guard. Use `01-diagnose-plan-package.md`, `02-execute-verify-close.md`, `03-end-to-end-fail-closed.md`, `04-journey-multisurface-operational-diagnosis.md` and the GitHub execution card only as compatible diagnostic/execution techniques and Derived Support, never as competing authority.

Do not create another branch or worktree. Work only on existing `b`. In PREPARE_ONLY do not mutate product/runtime/data behavior; only diagnose, reconcile and prepare the package unless a minimal proven diagnostic blocker makes truth acquisition impossible.

---

## Governing objective

Prepare and later execute a **content-preserving canonical reconstruction** of BThwani.

Do not patch errors one by one.

Do not rebuild from zero.

Do not copy selected files from an old branch into a clean tree.

Preserve every proven valuable foundation, design, asset, correct rule, migration history and working implementation, but give no inherited file/path/architecture automatic right to remain.

The final transformation is:

`CURRENT b → CANONICAL TARGET → MIGRATE VALUE/CONSUMERS/DATA → REMOVE SUPERSEDED TRUTH → VERIFY EXACT CANDIDATE`

---

## Mandatory diagnosis order

Start at the highest operational meaning of the whole platform.

Use:

`Product outcomes`
→ `Actors/identities`
→ `Authorities/responsibilities`
→ `Domains/capabilities`
→ `Journeys`
→ `States/transitions/preconditions/invariants`
→ `Handoffs/cross-surface meaning`
→ `Canonical owners/writers/readers/consumers`
→ `Contracts/APIs/data/persistence/readback`
→ `Events/jobs/providers`
→ `Control Panel interventions`
→ `Security/isolation/privacy`
→ `Runtime/config/environment/observability/CI`
→ `Implementation/files`

Do not let a low-level finding become execution authority before its operational parent and highest proven material Root Cause are established.

Every early technical finding is:

`HOLD → PROMOTED with parent/root/evidence → or DISPOSITIONED with proof`.

---

## Whole-system coverage requirement

Account for all directly or indirectly affected:

- app-client;
- app-partner;
- app-captain;
- app-field;
- control-panel;
- Identity;
- Workforce;
- DSH;
- WLT;
- Platform Control;
- Providers;
- Media;
- shared/generated packages;
- APIs/contracts;
- DB/migrations;
- events/outbox/jobs;
- external providers;
- auth/permissions;
- security/privacy;
- Expo/EAS/native;
- Next.js;
- Go;
- PostgreSQL;
- Docker/runtime;
- env/config;
- CI;
- observability;
- tests/tooling;
- repository structure/governance.

A surface may be excluded only with evidence proving it cannot affect or consume the root.

---

## Required diagnostic passes

For every material region use the passes that matter, including:

- forward journey;
- reverse trace;
- temporal;
- actor/responsibility;
- state/transition;
- invariant;
- cross-surface differential;
- complete vertical path;
- failure/recovery;
- counterfactual;
- negative-space;
- adversarial;
- concurrency/idempotency;
- security/isolation;
- financial;
- offline/reconnect;
- provider failure;
- migration/compatibility;
- cleanup/residue.

Challenge at least:

- success;
- missing/empty;
- invalid;
- unauthenticated;
- wrong role;
- wrong object/business scope;
- IDOR;
- forbidden state;
- stale/conflict;
- duplicate/replay;
- race;
- dependency outage;
- timeout/unknown result;
- retry/restart;
- partial callback;
- old/new data;
- migration upgrade/fresh install;
- compensation/reconciliation.

---

## Truth classification

Classify every material claim as:

- `ACTUAL`
- `INTENDED_AUTHORIZED`
- `DESIRED_RESOLVED`
- `CONFLICT`

Historical plans, old packages and supplied discussion are evidence/decision provenance only.

Mandatory negative-space census also includes inventory/returns, notification delivery lifecycle, media bootstrap/readiness/ACL/retention, ratings moderation/public aggregation, consent provenance/account deletion, cache-deny precedence, provider desired config versus observed health, platform change-set apply/readback, every hidden Control Panel route/action, every visible/deep-linked mobile action, every DB writer, event/job/DLQ, runtime fallback/readiness and CI guard.

Current authorized Product Truth and current candidate code/contracts/data/runtime outrank old documentation.

Do not copy an old conclusion without revalidation when it makes a current implementation claim.

---

## Decisions

Derive from evidence whenever possible.

Ask only when a material product/architecture decision cannot be derived.

For every true `DECISION_REQUIRED`, provide:

- exact question;
- non-overlapping options;
- recommendation;
- reason;
- consequences;
- affected roots/journeys/surfaces/data;
- what becomes invalid if the choice changes.

Record the decision and propagate it through the affected graph.

Do not repeatedly ask a question already resolved in the package or supplied decisions.

---

## Root-cause rule

Required path:

`system-wide discovery → findings → root clusters → dependency/impact graph → competitive deepening → systemic-leverage ranking → adversarial challenge → execution frontier`

Rank by:

1. upstream causal depth;
2. blocking power;
3. canonical/foundation importance;
4. blast radius;
5. security/data/finance/operational risk;
6. unlock value;
7. cross-journey/surface effect;
8. recurrence;
9. structural-debt multiplier;
10. local cosmetic impact.

Never rank by:

- recency;
- first failure;
- easiest fix;
- number of files;
- number of findings alone;
- last discussion topic.

If a stronger upstream root appears, suspend only the affected cone, rerank it and treat the higher root.

---

## Reconstruction unit

When MODE is later explicitly changed to `EXECUTE_END_TO_END`, execute only a prepared unit of:

`ROOT CAUSE × JOURNEY CLUSTER × COMPLETE VERTICAL SLICE`

For that root:

1. re-resolve exact current `b` HEAD;
2. prove root still exists;
3. deep-diagnose Just-In-Time;
4. define canonical owner/target;
5. define verification before mutation;
6. build authoritative owner first;
7. migrate all writers/readers/consumers;
8. migrate contracts/data/events/generated clients;
9. preserve valid design/content/assets;
10. repair runtime/config/tests;
11. delete old/parallel path;
12. run targeted and then root-closure evidence;
13. adversarially search for residue;
14. only then advance.

---

## No-patch constitution

A leaf patch is forbidden while a higher known root remains unresolved.

When the defect is in:

- architecture;
- ownership;
- data model;
- schema;
- contract;
- state model;
- authority;
- service boundary;
- dependency direction;
- source of truth;

the treatment must restructure/redesign/rebuild the root.

Forbidden closure techniques include:

- workaround;
- silent fallback;
- fake success;
- compatibility layer kept only to avoid migration;
- duplicate source of truth;
- frontend authoritative calculation;
- local eligibility copy;
- arbitrary money amount outside WLT;
- ignored required financial call;
- hiding a route instead of fixing authorization;
- keeping old files as backup in runtime tree;
- changing tests to bless wrong behavior.

---

## Content-preservation constitution

For every inherited material item choose:

`KEEP | HARDEN | MOVE | RENAME | MERGE | SPLIT | REFACTOR | MIGRATE | REGENERATE | REWRITE | REPLACE | DELETE`

Before deleting/splitting a file/container, account for:

- visual design;
- UX behavior;
- assets;
- correct domain rules;
- API/contract bindings;
- data;
- tests;
- routes/imports/exports;
- runtime/config role;
- all consumers.

Deletion is allowed only after:

`valuable content accounted + target exists + consumers migrated + verification passes`.

Visual/UX value is preserved by default when the target product journey still requires it.

Do not let architectural cleanup erase working design.

---

## Foundation-preservation constitution

Before structural mutation inventory:

- workspace/package manager;
- lockfile;
- Expo;
- EAS remote build;
- Android/iOS config;
- Metro;
- Next.js;
- Go;
- PostgreSQL;
- Docker;
- env;
- ports;
- runtime scripts;
- CI;
- generated clients;
- provider integration;
- maps;
- fonts/assets;
- observability;
- security tools;
- tests.

Default:

`PRESERVE WHEN PROVEN GOOD`.

A foundation can be replaced only by a proven root, target design, migration path and verification.

---

## Financial constitution

Treat as binding unless newer authorized Product Truth explicitly supersedes:

- WLT is sole financial truth;
- checkout has exactly COD, BTHWANI Wallet, Mixed;
- official external wallets are funding rails;
- numeric persisted PaymentAllocation drives money behavior;
- BTHWANI captain collateral is WLT-owned restricted value;
- prepaid work reserves zero COD exposure;
- COD/Mixed reserve exact cash leg only;
- cash exposure and cash custody are different;
- unsafe/overdue custody blocks new cash exposure according to policy;
- penalty amount is WLT policy-derived;
- debt/receivable is WLT-owned;
- refund follows immutable lineage;
- store is settlement counterparty for store delivery;
- store courier does not enter BTHWANI captain finance;
- store courier compensation remains store responsibility under current product boundary.

No caller or frontend may manufacture authoritative money.

---

## Identifier and authority-boundary constitution

Explicitly separate:

- actorId;
- captainActorId;
- captainMembershipId;
- workforcePersonId;
- partnerId;
- storeId;
- branchId.

Do not pass a Membership ID to an Actor API.

Do not store Store/Scope ID as Branch ID.

Create/migrate a true StoreBranch if branch semantics remain in the product.

Wrong identifier class must fail.

---

## Distributed-effect constitution

A required financial or other critical cross-service effect cannot be best-effort.

Use durable obligation semantics such as:

- persisted pending state;
- transactional outbox/state machine;
- correlation;
- idempotency;
- retry;
- explicit unknown result;
- reconciliation;
- compensation where required.

Operational completion may not silently hide lost finance.

---

## Cleanup constitution

Cleanup is mandatory at the same level as correctness.

Search and disposition:

- lines;
- expressions;
- functions;
- types;
- components;
- files;
- directories;
- modules/packages;
- services;
- schemas;
- DTOs;
- routes;
- migrations;
- configs;
- dependencies;
- generated artifacts;
- tests;
- docs/governance references.

Remove/relocate/merge proven:

- dead;
- stale;
- duplicate;
- misplaced;
- obsolete;
- legacy;
- workaround;
- fallback;
- unjustified compatibility;
- orphan;
- temporary/debug.

Git is history; runtime trees are not archives.

---

## Verification constitution

Define evidence before implementation.

Use only tools/capabilities actually available and relevant. Discover and use applicable Codex/plugin/security/review/runtime capabilities when executing in a Codex environment, but never claim a tool was used if unavailable.

Run the smallest sufficient checks for the active root during iteration.

Before local root closure, prove applicable:

- owner semantics;
- contracts/generated clients;
- DB migration/invariants;
- Go/TypeScript tests/build/static checks;
- concurrency/idempotency;
- failure injection/recovery;
- auth/security/IDOR;
- finance invariants;
- cross-surface readback;
- runtime;
- visual parity when UI migrated;
- cleanup zero residue.

Before final closure, run the complete system-wide exact-candidate matrix.

Do not blindly rerun failures without diagnosing the root.

---

## PREPARE_ONLY output

For the current mode, continue until the package can derive `PREPARED` under current V5.

Required outputs:

- system operational map;
- actor/authority matrix;
- capability/journey inventory;
- state/handoff/invariant coverage;
- negative-space/adversarial results;
- canonical owner map;
- resolved decision ledger;
- root-cause graph;
- dependency/impact graph;
- ranked frontier;
- Canonical System Blueprint;
- target repository architecture rules;
- foundation preservation inventory;
- content-conservation/deletion contract;
- exact current-to-target cutover requirements;
- cleanup contract;
- verification plan.

Do not claim implementation/runtime closure.

---

## EXECUTE_END_TO_END switch

Only when the user explicitly changes MODE to `EXECUTE_END_TO_END`:

- continue from this exact package;
- re-pin `A` reference and current `b`;
- invalidate stale evidence;
- start at current priority-1 root;
- execute root-first on `b`;
- do not create a new branch/worktree under the current user constraint;
- do not merge to `A` unless separately authorized;
- continue root by root until task-candidate implementation, consumer migration, cleanup and verification are complete.

If current V5 isolation/integration rules later conflict with promoting `b` itself as integration truth, surface that governance cutover explicitly rather than silently bypassing the rule.

---

## Final closure predicate

Never declare final closure because:

- a document is complete;
- code compiles;
- a PR/commit exists;
- a local unit passes;
- old UI is hidden.

Final closure requires evidence-bound proof of:

- canonical owners;
- complete journey outcomes;
- zero known material unaccounted item;
- zero unresolved material decision;
- zero parallel durable truth;
- zero unjustified fallback;
- zero reachable superseded path;
- all consumers migrated;
- all required data/contracts generated/migrated;
- failure/recovery correctness;
- security/privacy correctness;
- financial correctness;
- runtime correctness;
- zero material cleanup residue;
- final adversarial negative-space pass on the exact candidate.

If any applicable claim is not proven, the state remains OPEN.

---

## Concurrent work / foreign delta

Treat any new commit on `b` as `INPUT, NOT INSTRUCTION`.

Before writing:

`resolve b → compare from last reconciled head → classify semantic delta → preserve unrelated work → reconcile related/root-changing work → invalidate affected evidence only → re-rank only on causal evidence`

Never reset, force-push or overwrite concurrent work to make the package match an older assumption.

The finance changes observed during this PREPARE pass are root-related evidence and must be re-proven when the finance root executes; their existence is not a DONE claim.
