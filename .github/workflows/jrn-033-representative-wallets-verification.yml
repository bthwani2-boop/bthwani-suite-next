name: JRN-033 Representative Wallets Verification

on:
  workflow_dispatch:
  push:
    branches: [sambassam]
    paths:
      - "services/wlt/backend/internal/wallet/**"
      - "services/dsh/backend/internal/wlt/finance_proxy.go"
      - "services/dsh/backend/internal/wlt/client_test.go"
      - "services/dsh/backend/internal/wlt/representative_wallet_test.go"
      - "services/dsh/backend/internal/http/representative_finance_routes.go"
      - "services/dsh/backend/internal/http/representative_finance_routes_test.go"
      - "services/dsh/backend/internal/http/catalog_unified_routes.go"
      - "services/dsh/frontend/shared/finance-wlt-link/actor-wallet/**"
      - "services/dsh/frontend/shared/finance-wlt-link/field-finance/**"
      - "services/dsh/frontend/shared/finance-wlt-link/wlt/generated/WltDshPartnerBridge.tsx"
      - "services/dsh/frontend/app-client/account/MySpaceScreen.tsx"
      - "services/dsh/frontend/app-captain/account/DshCaptainFinanceScreen.tsx"
      - "services/dsh/frontend/app-field/finance/DshFieldFinanceScreen.tsx"
      - "services/dsh/frontend/control-panel/finance/**"
      - "apps/control-panel/runtime/src/app/dsh/finance/page.tsx"
      - "services/dsh/contracts/jrn-033-representative-finance.openapi.yaml"
      - "services/dsh/tests/jrn-033-representative-wallets-governance.test.mjs"
      - "services/dsh/tsconfig.jrn-033.json"
      - "governance/product/contracts/jrn-033-representative-wallets.product-truth.json"
      - ".github/workflows/jrn-033-representative-wallets-verification.yml"
  pull_request:
    paths:
      - "services/wlt/backend/internal/wallet/**"
      - "services/dsh/backend/internal/wlt/finance_proxy.go"
      - "services/dsh/backend/internal/wlt/client_test.go"
      - "services/dsh/backend/internal/wlt/representative_wallet_test.go"
      - "services/dsh/backend/internal/http/representative_finance_routes.go"
      - "services/dsh/backend/internal/http/representative_finance_routes_test.go"
      - "services/dsh/backend/internal/http/catalog_unified_routes.go"
      - "services/dsh/frontend/shared/finance-wlt-link/actor-wallet/**"
      - "services/dsh/frontend/shared/finance-wlt-link/field-finance/**"
      - "services/dsh/frontend/shared/finance-wlt-link/wlt/generated/WltDshPartnerBridge.tsx"
      - "services/dsh/frontend/app-client/account/MySpaceScreen.tsx"
      - "services/dsh/frontend/app-captain/account/DshCaptainFinanceScreen.tsx"
      - "services/dsh/frontend/app-field/finance/DshFieldFinanceScreen.tsx"
      - "services/dsh/frontend/control-panel/finance/**"
      - "apps/control-panel/runtime/src/app/dsh/finance/page.tsx"
      - "services/dsh/contracts/jrn-033-representative-finance.openapi.yaml"
      - "services/dsh/tests/jrn-033-representative-wallets-governance.test.mjs"
      - "services/dsh/tsconfig.jrn-033.json"
      - "governance/product/contracts/jrn-033-representative-wallets.product-truth.json"
      - ".github/workflows/jrn-033-representative-wallets-verification.yml"

permissions:
  contents: read

concurrency:
  group: jrn-033-representative-wallets-${{ github.sha }}
  cancel-in-progress: false

jobs:
  backend:
    name: WLT ownership and DSH actor scoping
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4

      - name: Set up Go from DSH module
        uses: actions/setup-go@v5
        with:
          go-version-file: services/dsh/backend/go.mod
          cache-dependency-path: |
            services/dsh/backend/go.sum
            services/wlt/backend/go.sum

      - name: Test WLT wallet validation
        working-directory: services/wlt/backend
        run: go test ./internal/wallet -count=1

      - name: Test DSH WLT wallet boundary
        working-directory: services/dsh/backend
        run: >-
          go test ./internal/wlt
          -run 'TestFinanceReadWallet'
          -count=1

      - name: Test own wallet actor scoping
        working-directory: services/dsh/backend
        run: go test ./internal/http -run '^TestRepresentativeOwnWalletRoutesResolveAuthenticatedActor$' -count=1 -v

      - name: Test own ledger actor scoping
        working-directory: services/dsh/backend
        run: go test ./internal/http -run '^TestRepresentativeOwnLedgerRoutesOverrideActorQuery$' -count=1 -v

      - name: Test operator wallet permission and validation
        working-directory: services/dsh/backend
        run: go test ./internal/http -run '^TestControlPanelRepresentativeWalletValidatesTypeAndUsesPermissionFallback$' -count=1 -v

      - name: Test operator ledger path-pinned scope
        id: operator-ledger-test
        continue-on-error: true
        working-directory: services/dsh/backend
        shell: bash
        run: |
          set -o pipefail
          go test ./internal/http -run '^TestControlPanelRepresentativeLedgerPinsActorAndNoStore$' -count=1 -v 2>&1 | tee /tmp/jrn-033-go-routes.log

      - name: Upload Go route diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jrn-033-go-route-diagnostics-${{ github.run_attempt }}
          path: /tmp/jrn-033-go-routes.log
          if-no-files-found: warn
          retention-days: 14

      - name: Fail when operator ledger test failed
        if: steps.operator-ledger-test.outcome == 'failure'
        run: exit 1

      - name: Build affected financial owners
        working-directory: services/dsh/backend
        run: go build ./internal/wlt ./internal/http

  frontend:
    name: Shared brain and all representative surfaces
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "10.34.2"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.17.0"
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Run JRN-033 ownership and route guard
        run: node --test services/dsh/tests/jrn-033-representative-wallets-governance.test.mjs

      - name: Typecheck the JRN-033 multi-surface ownership graph
        id: focused-typecheck
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm --dir services/dsh exec tsc -p tsconfig.jrn-033.json --noEmit --pretty false 2>&1 | tee /tmp/jrn-033-typescript.log

      - name: Upload TypeScript diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jrn-033-typescript-diagnostics-${{ github.run_attempt }}
          path: /tmp/jrn-033-typescript.log
          if-no-files-found: error
          retention-days: 14

      - name: Fail when focused typecheck failed
        if: steps.focused-typecheck.outcome == 'failure'
        run: exit 1

  contract:
    name: Focused contract and product truth
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "10.34.2"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.17.0"
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Parse focused OpenAPI through the repository generator
        run: pnpm --dir services/dsh exec openapi-typescript contracts/jrn-033-representative-finance.openapi.yaml -o /tmp/jrn-033-representative-finance-api.ts

      - name: Validate product truth against the governed schema
        run: >-
          node --input-type=module -e
          "import fs from 'node:fs'; import Ajv from 'ajv';
          const schema=JSON.parse(fs.readFileSync('governance/product/product-truth.schema.json','utf8'));
          const truth=JSON.parse(fs.readFileSync('governance/product/contracts/jrn-033-representative-wallets.product-truth.json','utf8'));
          const validate=new Ajv({allErrors:true,strict:false}).compile(schema);
          if(!validate(truth)){console.error(validate.errors);process.exit(1)}
          if(truth.capabilityId!=='JRN_033_REPRESENTATIVE_WALLETS_REFERENCE_FINANCE'||truth.state!=='DISCOVERY')process.exit(1);"

      - name: Verify repository whitespace
        run: git diff --check
