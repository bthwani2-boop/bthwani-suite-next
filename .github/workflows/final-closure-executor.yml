name: Final Closure Executor

on:
  push:
    branches:
      - "fix/lian-sovereign-final-closure"
    paths:
      - ".github/workflows/final-closure-executor.yml"

permissions:
  contents: write

concurrency:
  group: final-closure-executor
  cancel-in-progress: false

jobs:
  execute:
    name: Apply verify and commit final repair
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout exact closure branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: fix/lian-sovereign-final-closure
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup governed workspace
        uses: ./.github/actions/setup-node-workspace

      - name: Apply deterministic repair
        run: python3 tools/scripts/apply-final-closure-repair.py

      - name: Format Go source
        run: gofmt -w tools/guards/extract_routes.go services/dsh/backend/internal/http/server.go services/dsh/backend/internal/wlt/subscription_payment_bound.go

      - name: Regenerate governed clients
        run: |
          pnpm run openapi:generate:dsh
          pnpm run openapi:generate:wlt

      - name: Verify repaired contracts and governance
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/final-closure-executor
          pnpm run guard:partner-surface-truth 2>&1 | tee artifacts/final-closure-executor/partner-surface.log
          pnpm run guard:guard-registry 2>&1 | tee artifacts/final-closure-executor/guard-registry.log
          pnpm run guard:wlt-financial-boundary 2>&1 | tee artifacts/final-closure-executor/wlt-boundary.log
          pnpm run guard:api-binding 2>&1 | tee artifacts/final-closure-executor/api-binding.log
          pnpm run guard:workflow-lint 2>&1 | tee artifacts/final-closure-executor/workflow-lint.log
          pnpm run guard:actions-pin 2>&1 | tee artifacts/final-closure-executor/actions-pin.log
          pnpm run swagger:build 2>&1 | tee artifacts/final-closure-executor/swagger.log
          pnpm run guard:backend-api-binding 2>&1 | tee artifacts/final-closure-executor/backend-api-binding.log
          rm -rf dist-swagger

      - name: Upload verification logs
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: final-closure-executor-${{ github.sha }}
          path: artifacts/final-closure-executor
          if-no-files-found: warn
          retention-days: 3

      - name: Commit verified repair
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "bthwani-closure-executor"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(closure): converge contracts routes and governance"
          git push origin HEAD:fix/lian-sovereign-final-closure
