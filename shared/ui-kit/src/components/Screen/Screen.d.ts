import type { ReactNode } from "react";
export type ScreenProps = {
    children?: ReactNode;
    padded?: boolean;
    centered?: boolean;
    maxWidth?: number;
};
export declare function Screen({ children, padded, centered, maxWidth }: ScreenProps): import("react").JSX.Element;
//# sourceMappingURL=Screen.d.ts.map