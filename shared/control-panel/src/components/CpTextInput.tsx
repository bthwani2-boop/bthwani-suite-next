import { useCpTokens } from "./cpTokens";
export type CpTextInputProps = { readonly value: string; readonly onChange: (value: string) => void; readonly placeholder?: string; readonly type?: "text" | "password" | "date"; readonly disabled?: boolean; readonly "aria-label"?: string; };
export function CpTextInput({ value, onChange, placeholder, type = "text", disabled, "aria-label": ariaLabel }: CpTextInputProps) { const { styles } = useCpTokens(); return <input type={type} value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} disabled={disabled} style={styles.textInput} aria-label={ariaLabel} />; }
export type CpTextAreaProps = { readonly value: string; readonly onChange: (value: string) => void; readonly placeholder?: string; readonly disabled?: boolean; readonly rows?: number; readonly "aria-label"?: string; };
export function CpTextArea({ value, onChange, placeholder, disabled, rows = 4, "aria-label": ariaLabel }: CpTextAreaProps) { const { styles } = useCpTokens(); return <textarea value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} disabled={disabled} rows={rows} style={{ ...styles.textInput, resize: "vertical", fontFamily: "inherit" }} aria-label={ariaLabel} />; }
