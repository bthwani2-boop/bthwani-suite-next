# Final Gate

- Status: `NOT_CLOSED` — live-code remediation landed and is verified at the build/typecheck/unit-test level, but DB-dependent and runtime smoke checks were not executed in this session (no live Postgres/backend running here). Do not flip this to `CLOSED` until phases 1, 8, 11, 12, and 13 in `13_VERIFICATION_MATRIX.md` are actually run against a live environment.
- What changed this pass (see `12_CLEANUP_LEDGER.md` for full detail):
  - App-field no longer uses the free-form local product flow; it was calling backend routes (`/dsh/field/partners/{partnerId}/products*`) that had already been removed from `server.go` — a live 404 bug, not just a governance violation. It now uses the already-existing central-catalog field APIs (taxonomy browse, master-product search, store-assortment link, product-proposal submission).
  - `catalog.types.ts`/`catalog.api.ts` (legacy `PartnerCatalog` shape) split into clearly labeled `legacy-catalog-compat.*` files; sovereign truth stays exclusively in `central-catalog.*`.
  - Cart (`dsh_cart_items`) gained `master_product_id`/`store_assortment_id` (migration `dsh-033`); `internal/cart.UpsertItem` now validates against `dsh_store_assortments`+`dsh_master_products` instead of the legacy `dsh_catalog_products` table it was silently failing against for any central-catalog product.
  - Control-panel: 182 inline-style violations reduced to 6 (all documented dynamic exceptions); the orphaned `CategoryControlRoom` (real category CRUD) is now wired into the taxonomy tab; the orphaned, fully-simulated `CatalogWorkspaceDrawers.tsx` (625 lines, no real API calls) was deleted rather than "fixed".
  - DAM UI: upload form, entity-image-link form, archive action, and a client-derived "missing image" badge added to the marketing/media tab, using the previously-unused `uploadAndLinkAsset`/`putEntityImage`/`fetchCatalogAssetLinks` API functions.
  - Backend permissions: additive `requireCatalogPermission` checks 9 named actions against `identity.Permissions` (already parsed by the in-repo identity service, previously never consulted) with `operator` role kept as an unconditional fallback — zero behavior change for existing operators, but a real path now exists to grant a narrower non-operator permission (e.g. media review only) without full operator elevation.
- Verified this pass: `go build ./...`, `go vet ./...`, `go test ./... -count=1` (all packages pass) in `services/dsh/backend`; `pnpm -w run typecheck` (0 errors across every workspace); `pnpm -w run openapi:generate` (dsh/wlt/identity clients regenerated, cart schema updated).
- Not verified this pass (documented, not hidden): DB migration `dsh-033` has not been applied/verified against a live Postgres; the cart DB-integration test was updated but not executed; DAM upload/link flow and the granular-permission grant path were not exercised end-to-end against a running backend + identity DB; the partner→client-visible pipeline smoke sequence was not run.
- Remaining blockers before `CLOSED` is honest:
  1. Apply `dsh-033` and run `verify-central-catalog-seed.sql` + `GET /dsh/operator/catalog/seed-status` against a live DB.
  2. Run the pipeline smoke sequence (proposal → …→ client-visible, with barcode/image/manual_request policy rejections) end-to-end.
  3. Exercise the DAM upload/link UI against a running backend + object store.
  4. Grant at least one non-operator identity a scoped `catalog.*` permission and confirm it works (and that operators are unaffected).
