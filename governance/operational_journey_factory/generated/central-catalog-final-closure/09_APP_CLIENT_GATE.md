# App Client Gate

- Client application fetches client visible catalog using `fetchPublishedCatalog` mapping from `/dsh/stores/{storeId}/catalog`.
- Integrates domains, nodes, and products client-side mapping directly.
- Ensures only approved master products with active assortments and satisfied image policies are rendered to the client.
