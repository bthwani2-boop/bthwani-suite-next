# J004 — إصدار التفعيل وإلغاؤه وقراءته

```yaml
owner: Identity
actors: [OPERATOR, PARTNER, CAPTAIN, FIELD]
surfaces: [control-panel, app-partner, app-captain, app-field]
control_panel_sections: [administration, hr, partners]
dependencies: [FOUNDATION-00, J003]
assessment_status: NOT_ASSESSED
closure_status: OPEN
```

## النتيجة التشغيلية

إصدار رمز تفعيل قصير العمر لActor موجود ومؤهل للسطح، إظهاره مرة واحدة فقط، ثم إلغاؤه أو إعادة إصداره دون وجود رمزين فعالين خارج السياسة.

## الحالة الحالية

| بند | الحكم الحالي |
|---|---|
| endpoints التفعيل | مثبتة إجمالًا، تحتاج mapping دقيق |
| raw code handling | غير مثبت في logs/storage/responses |
| single-use/hash/expiry | يجب إثباته من DB/handler |
| free-phone issuance | يجب منعه لصالح اختيار Actor موجود |
| revoke/reissue races | غير مثبتة |

### الناقص والخاطئ المطلوب كشفه

- إدخال هاتف حر بدل اختيار Actor.
- إصدار رمز لسطح أو role غير متوافق.
- تخزين raw code أو إظهاره بعد أول response.
- أكثر من رمز فعال أو عدم إبطال السابق.
- عدم وجود attempt limit/audit/correlation.

## Control Panel

- entry من صفحة Actor/Workforce profile لا form هاتف منفصل.
- يعرض الاسم/Actor ID/role/surface/status قبل الإصدار.
- زر `إصدار رمز` مع confirmation.
- response يعرض raw code مرة واحدة مع زر Copy accessible وتحذير.
- بعد إغلاق modal أو refresh يعرض masked code وexpiry/status فقط.
- أزرار `إلغاء` و`إعادة إصدار` حسب الحالة.
- حالات: no actor، wrong role، already active، revoked، expired، rate limited، unknown result.

## أقسام لوحة التحكم المرتبطة — التحديد التنفيذي الإلزامي

| القسم والمسار | الصفحة/التبويب | ما يجب أن يظهر | التحكمات والأثر | الحالات والصلاحيات | الربط والاختبار اليدوي |
|---|---|---|---|---|---|
| **الإدارة والصلاحيات** `/dsh/administration` | Actor Detail → تبويب `التفعيل` | target Actor/role/surface/status، masked code metadata، expiry/attempts/issuer/audit | `إصدار رمز`، `نسخ مرة واحدة`، `إلغاء`، `إعادة إصدار`، `تحديث الحالة` | issue/read/revoke/reissue permissions؛ wrong-role/already-active/revoked/expired/rate-limited/unknown | Identity operations المنفصلة؛ اختبر double issue، modal close، refresh، raw-code scan |
| **الموارد البشرية** `/dsh/hr` | Captain/Field Profile → بطاقة `التفعيل` | readiness المهنية وActor reference وحالة الرمز masked؛ لا هاتف حر | `إصدار/إعادة إصدار` يظهر فقط كتفويض للعملية الحاكمة بعد profile readiness؛ `فتح الإدارة` | HR provisioning permission + Identity issue permission؛ incomplete/suspended/wrong-surface | اختر موظفًا موجودًا، أصدر الرمز، ألغِه، وتحقق من عدم إنشاء Actor أو رمز موازٍ |
| **الشركاء والمتاجر** `/dsh/partners/[partnerId]` | Partner/Team onboarding → `التفعيل والدخول` | عضو/Actor المختار، surface، onboarding state، masked activation status | `إصدار` و`إعادة إصدار` يستدعيان Identity command نفسها؛ لا input هاتف مستقل | partner team scope + activation permission؛ duplicate/active/revoked/unknown | أصدر لعضو Partner، جرّب Actor من شريك آخر وsurface خاطئ، راجع audit |

Dashboard وOperations لا تصدر رموزًا؛ تعرض blocker أو alert ورابطًا للقسم المالك فقط.

## App Partner/Captain/Field

- لا تصدر الرموز.
- شاشة التفعيل تستقبل الرمز في J005.
- عند إلغاء الرمز أثناء الإدخال يظهر رد محدد ولا retry عمياء.

## التقنية

- OpenAPI operationIds مستقلة: issue/read/revoke/reissue.
- raw code لا يكتب في DB أو logs؛ hash مضبوط بخوارزمية مناسبة.
- unique active-code policy وexpiry/attempt counters.
- idempotency لإصدار الرمز وresult lookup بعد فقد الرد.
- notification delivery إن وجدت منفصلة عن حقيقة الرمز.
- audit يتضمن issuer/target/surface/reason دون raw code.

## الاختبارات

- issue لActor مفقود/معلق/wrong surface.
- concurrent issue/revoke/reissue.
- same idempotency key.
- raw-code scans للlogs والDB.
- expiry boundary وclock skew.
- copy modal ثم refresh.
- عدم وجود free-phone form أو write path إضافي في الأقسام الثلاثة.

## التجريب اليدوي

1. افتح Actor مؤهلًا من `/dsh/administration`.
2. أصدر رمزًا وانسخه، ثم أغلق modal وأعد الصفحة.
3. كرر من `/dsh/hr` و`/dsh/partners/[partnerId]` وتحقق أن العملية والنتيجة نفسها.
4. تحقق من masking وعدم وجود raw code في Network/logs بعد الاستجابة الأولى.
5. ألغِ الرمز وحاول استخدامه.
6. أعد الإصدار وتأكد من إبطال السابق.
7. نفّذ نقرًا مزدوجًا وفقد الرد ثم تحقق من النتيجة.

## الإغلاق

`free_phone_issue_paths=0; raw_codes_at_rest_or_logs=0; active_code_policy=PASS; issue_revoke_races=PASS; one_time_visibility=PASS; unmapped_control_panel_sections_or_controls=0; evidence_sha=CURRENT_SHA`.
