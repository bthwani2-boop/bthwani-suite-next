# J013 — جاهزية الميداني

```yaml
owners: [Workforce, DSH]
actors: [OPERATOR, FIELD]
surfaces: [control-panel, app-field]
control_panel_sections: [hr, operations, partners, administration]
dependencies: [FOUNDATION-00, J012]
assessment_status: NOT_ASSESSED
closure_status: OPEN
```

## النتيجة التشغيلية

تمكين Field فقط عندما يكون Actor وprofile وemployment وshift وassignment للمنطقة/الشريك/المتجر فعالين، وإعادة التحقق عند كل mutation وعند reconnect.

## الحالة الحالية

- **الموجود:** app-field وWorkforce/DSH scope concepts.
- **غير المثبت:** gate موحد، effective ranges، offline revoke behavior، task filtering.
- **الخاطئ المحتمل:** profile role فقط، hardcoded field user، local area IDs، mutation بعد انتهاء assignment.
- **التصحيح:** readiness endpoint وtrusted assignment context وbackend recheck.
- **الإضافات:** blocked reasons، shift/timezone handling، task-cache invalidation.

## Control Panel

- Field profile checklist: Identity، employment، supervisor، shift، city، active assignments، documents.
- assignment create/end/history مع effective time.
- لا زر `تفعيل` عام يتجاوز النواقص؛ يعرض كل blocker.

## أقسام لوحة التحكم المرتبطة — التحديد التنفيذي الإلزامي

| القسم والمسار | الصفحة/التبويب | ما يظهر | التحكمات | الحالات والصلاحيات | الاختبار |
| --- | --- | --- | --- | --- | --- |
| **الموارد البشرية** `/dsh/hr` | Field Detail → `الجاهزية`، `المناوبة`، `التكليفات`، `المستندات` | employment/supervisor/shift/city/documents/assignment versions وeffective ranges | create/end assignment، set shift/supervisor، request docs؛ لا Ready toggle | HR field-level permissions؛ incomplete/no-shift/no-assignment/expired/suspended | اختبر future/expired assignment وshift boundary وdocument state |
| **العمليات** `/dsh/operations` | Field work queue/task detail/readiness banner | DSH task scope، active area/store/partner assignment، current allowedActions | claim/execute actions فقط إذا fresh readiness؛ `فتح HR` | operations object scope؛ outside-area/stale/offline/forbidden | بدّل task IDs، أنهِ assignment أثناء page/offline ثم reconnect |
| **الشركاء** `/dsh/partners/[partnerId]` و`/stores` | Partner/Store → `الفريق الميداني` أو assignment refs | Field assignments الخاصة بالشريك/المتجر وحالتها فقط | request/assign ضمن orchestration وصلاحية؛ لا HR profile edit | partner/store scope؛ cross-partner denied | حاول ربط Field بمتجر خارج scope، اختبر end/history |
| **الإدارة** `/dsh/administration` | Actor status/role/sessions | Identity blocker منفصل عن readiness المهنية | open/suspend/revoke عبر Identity؛ لا assignment edit | Identity permissions | علّق Actor وتحقق من HR/Operations/App Field وqueue invalidation |

Finance غير منطبق على جاهزية Field هنا إلا إذا رحلة مالية لاحقة تضيف قرارًا صريحًا؛ لا يُفترض blocker مالي محلي.

## App Field

- شاشة readiness قبل work queue.
- يعرض supervisor/shift/areas/tasks الحالية فقط.
- حالات: no assignment، shift inactive، outside area، profile incomplete، suspended، offline stale.
- كل task/mutation يعرض scope الحالي.
- عند revoke أثناء offline: queue تبقى للعرض لكن لا ترسل؛ تظهر conflict/action expired.

## التقنية

- trusted context من Workforce assignments لا request headers.
- DSH task queries مفلترة بالassignment الفعال.
- version/effective time في mutations.
- local cache partitioned by actor+assignment version.
- no bootstrap account in production path.

## الاختبارات

- assignment missing/expired/future.
- shift boundary/timezone.
- cross-area/store IDs.
- revoke during offline queue.
- supervisor/role mismatch.
- app reinstall/cache stale.
- كل Control Panel action مرتبط بمالك واحد وobject scope.

## التجريب اليدوي

1. فعّل Field بلا assignment ثم أضف assignment من HR.
2. ابدأ shift وافتح work queue في Operations/App Field.
3. حاول task خارج area/store وPartner scope آخر.
4. ألغِ assignment أثناء offline وأرسل action بعد reconnect.
5. اختبر انتهاء shift وActor suspension من Administration.
6. تحقق من history/audit في الأقسام الأربعة.

## الإغلاق

`field_readiness_bypasses=0; cross_area_store_actions=0; stale_offline_privileged_actions=0; local_scope_truth=0; all_blocked_states_visible=PASS; unmapped_control_panel_sections_or_controls=0; evidence_sha=CURRENT_SHA`.
