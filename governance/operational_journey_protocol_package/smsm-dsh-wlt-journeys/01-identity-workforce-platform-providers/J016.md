# J016 — Progressive Rollout وKill Switch

```yaml
owner: Platform Control
actors: [OPERATOR, CUSTOMER, PARTNER, CAPTAIN, FIELD]
surfaces: [control-panel, app-client, app-partner, app-captain, app-field]
control_panel_sections: [platform, dashboard, operations, partners, marketing, finance]
dependencies: [FOUNDATION-00, J015]
assessment_status: NOT_ASSESSED
closure_status: OPEN
```

## النتيجة التشغيلية

قرار rollout حتمي لنفس Actor/context عبر جميع الخدمات والأسطح، مع targeting/effective time/percentage وkill switch مركزي فوري، وصفر feature flags محلية موازية.

## الحالة الحالية

- يجب جرد كل flags/constants/env checks في المستودع.
- غير مثبت أن القرار deterministic وأن cache invalidation فوري.
- الخطر: نفس Actor يرى feature في Surface ولا يراه في آخر، أو kill switch يوقف UI فقط بينما Backend يستمر.
- المطلوب: decision contract/service، stable hashing، server enforcement، diagnostics.

## Control Panel

- قائمة capabilities/rollouts والحالة والtargets والنسخة.
- create/edit/schedule/pause/kill مع diff وapproval حسب J015.
- preview actor/partner/store decision دون منح صلاحية.
- metrics exposure مع حماية من bias/PII.

## أقسام لوحة التحكم المرتبطة — التحديد التنفيذي الإلزامي

| القسم والمسار | الصفحة/التبويب | ما يظهر والتحكمات | الحالات والصلاحيات | الاختبار |
|---|---|---|---|---|
| **المنصة** `/dsh/platform` و`/policies` | Capabilities/Rollouts/Kill Switch/Decision Preview | version/targets/percentage/hash algorithm/effective time/cache ack؛ create/schedule/pause/kill/rollback/preview | change-set permissions وapproval؛ draft/scheduled/active/paused/killed/partial/stale | boundaries 0/1/99/100، deterministic Actor، cache/event loss، kill أثناء request |
| **الرئيسية** `/dsh/dashboard` | rollout/kill alerts وconsumer adoption | current version، affected capabilities، partial consumers، freshness؛ links فقط | dashboard viewer؛ no kill control إلا deep link للمخول | طبق kill وتحقق من alert وعدم success الكاذب |
| **العمليات** `/dsh/operations` | capability badges/allowedActions على Orders/Dispatch | decision version/reason؛ actions disabled backend-enforced | operations permissions لا rollout edit؛ killed/stale/blocked | kill أثناء شاشة وoffline action ثم API مباشر |
| **الشركاء** `/dsh/partners/[partnerId]` | Partner/Store capability preview | decision للسياق partner/store/actor دون منافسين أو PII | preview فقط أو change request؛ لا local flag | نفس Actor عبر Partner app/CP وstore مختلف |
| **التسويق** `/dsh/marketing` | campaign/feature rollout context | audience/percentage/effectivity والdecision preview | marketing policy permission ضمن change set | اختبر targeting والتعارض مع campaign policy |
| **المالية** `/wlt/finance` | financial feature decision reference | WLT-owned decision/version للقدرات المالية؛ no financial truth in flag | finance/change-set separation؛ fail-closed للحساس | kill payment/payout capability واختبر DSH facade/Backend |

Catalogs/HR/Support تستخدم decision contract نفسها عندما تنطبق ولا تضيف env/local flag؛ يجب إثبات N/A أو mapping عند إضافة capability.

## التطبيقات

- navigation/control visibility تعتمد decision موثوقة.
- Backend يرفض command عند kill حتى لو UI stale.
- عند switch off أثناء الشاشة تظهر blocked state وتزال queued mutation.
- offline لا يفترض enabled إذا انتهت freshness لعملية خطرة.

## التقنية

- stable actor/context key وpercentage algorithm versioned.
- no random per request.
- decisions cached بTTL/version وinvalidated by event.
- kill switch fail-closed للقدرات الحساسة.
- flags لا تنقل financial truth أو authorization.

## الاختبارات

- نفس Actor عبر surfaces/services.
- boundaries 0/1/99/100%.
- start/end times.
- cache stale وevent loss.
- kill أثناء mutation.
- local flag grep/guard.
- section-by-section decision parity.

## التجريب اليدوي

1. فعّل capability لنطاق محدود من Platform.
2. افتحها عبر كل Surface والأقسام المرتبطة لنفس Actors مختلفين.
3. غيّر النسبة والوقت.
4. نفّذ kill أثناء شاشة مفتوحة وoffline.
5. استدع API مباشرة بعد kill.
6. تحقق من Dashboard/Operations/Partners/Marketing/Finance وعدم وجود flags محلية.

## الإغلاق

`local_feature_flags=0; inconsistent_decisions=0; backend_kill_enforcement=PASS; deterministic_targeting=PASS; stale_sensitive_actions=0; unmapped_control_panel_sections_or_controls=0; evidence_sha=CURRENT_SHA`.
