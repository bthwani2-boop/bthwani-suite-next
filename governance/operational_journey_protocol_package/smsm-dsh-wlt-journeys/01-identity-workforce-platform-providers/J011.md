# J011 — التزويد الإداري والموارد البشرية

```yaml
owners: [Workforce, Identity]
actors: [OPERATOR, PARTNER, CAPTAIN, FIELD]
surfaces: [control-panel, app-partner, app-captain, app-field]
control_panel_sections: [hr, administration, partners, operations]
dependencies: [FOUNDATION-00, J010]
assessment_status: NOT_ASSESSED
closure_status: OPEN
```

## النتيجة التشغيلية

Orchestration موثوق ينشئ Workforce person ثم Identity Actor ثم role/assignment المطلوب، أو يتعافى/يعوض دون ترك orphan في أي خدمة. لا يجوز أن ينفذ Control Panel سلسلة طلبات مستقلة بلا correlation وحالة قابلة للاستئناف.

## الحالة الحالية

- **الموجود:** Identity وWorkforce endpoints ومفاهيم activation/profile.
- **غير المثبت:** orchestrator واحد، durable provisioning case، compensation، idempotency عبر الخدمات.
- **الخاطئ المحتمل:** profile بلا Actor، Actor بلا profile، duplicate عند retry، UI يعتبر النجاح بعد أول خدمة فقط.
- **التصحيح:** provisioning case/state machine وservice identities وصلاحيات محددة وresult lookup.
- **الإضافات:** correlation، step status، retry/compensate controls، orphan reconciliation job.

## Control Panel

- Wizard واضح: نوع الشخص → البيانات المهنية → Actor identity → الدور → assignments → مراجعة → تنفيذ.
- كل خطوة تعرض validation ومصدر الحقيقة.
- شاشة progress تعرض `Workforce created`, `Actor created`, `Role assigned`, `Scope assigned`, `Activation ready`.
- إذا فشلت خطوة: لا يعرض نجاحًا كاملاً؛ يقدم `استئناف` أو `تعويض` حسب السياسة.
- زر submit واحد مع idempotency وunknown-result lookup.

## أقسام لوحة التحكم المرتبطة — التحديد التنفيذي الإلزامي

| القسم والمسار | الصفحة/التبويب | ما يظهر | التحكمات | الحالات والصلاحيات | العقود والتجريب |
|---|---|---|---|---|---|
| **الموارد البشرية** `/dsh/hr` | `إضافة شخص` Wizard وProvisioning Case Detail | person fields، Actor step، role، assignment، documents، step versions، correlation، orphan status | `حفظ مسودة`، `تحقق`، `تنفيذ`، `استئناف`، `تعويض`، `إعادة قراءة` | HR provision permission + step owner permissions؛ draft/validated/partial/retryable/compensation/complete/unknown | Workforce orchestration API؛ عطّل Identity بعد Workforce commit ثم resume/reconcile |
| **الإدارة والصلاحيات** `/dsh/administration` | Actor Detail وProvisioning Reference | Actor/role/session/activation refs وcase link؛ لا HR field copies | `فتح الحالة` و`فتح HR`؛ lifecycle actions وفق Identity فقط | Identity read/action permissions؛ orphan Actor/linked/partial | قارن case references، جرّب duplicate phone وresponse loss، تأكد من Actor واحد |
| **الشركاء** `/dsh/partners/[partnerId]` | Onboarding/Team → `تزويد عضو` | partner/store scope، professional person candidate، Actor/role/activation progress | يبدأ نفس provisioning case؛ لا سلسلة requests محلية | partner/team scope + HR/Identity orchestration permission | زوّد Partner employee ثم كرر من HR وتحقق من idempotency وعدم duplication |
| **العمليات** `/dsh/operations` | Provisioning/Readiness blockers في work items | الحالة النهائية أو partial blocker فقط، owner/next action | `فتح HR Case`؛ لا retry/compensate من Operations إلا permission صريحة | operations viewer؛ no sensitive HR/auth data | حاول استخدام Actor/Profile partial في assignment وتأكد من fail-closed |

Dashboard يعرض عدد الحالات العالقة فقط مع freshness؛ Platform يعرض صحة الـorchestrator/job ولا ينفذ التزويد.

## التطبيقات

- لا تنشئ الشخص أو Actor.
- بعد التفعيل تقرأ نتيجة provisioning؛ تعرض `PROFILE_INCOMPLETE` أو `ASSIGNMENT_MISSING` بدل إنشاء بيانات محلية.
- App Partner/Captain/Field لا تتجاوز حالة partial provisioning.

## نموذج الحالات

`DRAFT → VALIDATED → WORKFORCE_CREATED → ACTOR_CREATED → ROLE_ASSIGNED → SCOPE_ASSIGNED → READY_FOR_ACTIVATION → COMPLETED`، مع `FAILED_RETRYABLE`, `FAILED_COMPENSATION_REQUIRED`, `COMPENSATED`.

## التقنية

- service-to-service auth منفصلة.
- idempotency key على provisioning case.
- كل خطوة تحفظ external reference/version.
- compensation لا تحذف تاريخًا ماليًا/تشغيليًا؛ تستخدم deactivation/rollback مع audit.
- reconciliation job يكتشف orphans.
- UI لا ينسق الحقيقة بنفسه.

## الاختبارات

- Identity failure بعد Workforce commit والعكس.
- duplicate submit/response loss.
- compensation failure.
- stale case resume.
- permission/scope escalation.
- conflicting employee code/phone.
- تبويبات وأزرار الأقسام الأربعة تستخدم case واحدة.

## التجريب اليدوي

1. زوّد Field وCaptain وPartner employee من `/dsh/hr` وPartner onboarding.
2. عطّل Identity في منتصف التنفيذ.
3. أعد تشغيل الصفحة واضغط استئناف.
4. كرر submit وفقد الرد بعد commit.
5. افتح Administration وOperations وتحقق من reference/blocker فقط.
6. افحص كلا الخدمتين وreconciliation queue.
7. سجّل الدخول وتحقق من gate الصحيح.

## الإغلاق

`orphan_workforce_profiles=0; orphan_identity_actors=0; duplicate_provisioning_effects=0; recoverable_partial_cases=PASS; ui_false_success=0; unmapped_control_panel_sections_or_controls=0; evidence_sha=CURRENT_SHA`.
