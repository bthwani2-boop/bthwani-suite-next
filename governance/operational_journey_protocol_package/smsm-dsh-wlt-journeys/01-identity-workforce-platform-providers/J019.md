# J019 — اعتمادات المزود والمهل وCircuit Breaker

```yaml
owner: Providers
actors: [OPERATOR, SERVICE_IDENTITY]
surfaces: [control-panel, app-client, app-partner, app-captain, app-field]
control_panel_sections: [platform, dashboard, operations, finance, support]
dependencies: [FOUNDATION-00, J018]
assessment_status: NOT_ASSESSED
closure_status: OPEN
```

## النتيجة التشغيلية

كل اتصال خارجي يستخدم TLS وtimeout budget وretry classification وcircuit breaker وتوقيع webhook وresult reconciliation، دون retry storm أو نجاح كاذب.

## الحالة الحالية

- يلزم جرد كل HTTP/provider clients وwebhooks وtimeouts/retries.
- الخطر: calls بلا timeout، retry لكل 4xx/5xx، unsigned webhook، duplicate late result، breaker محلي غير مرئي.
- المطلوب: adapter policies موحدة، idempotency/provider reference، signature rotation، metrics.

## Control Panel

- provider connection status وbreaker state `CLOSED/OPEN/HALF_OPEN`.
- last success/failure/latency/error class.
- controls محدودة: test، maintenance، authorized retry/reconcile.
- لا زر يعيد financial/provider mutation عمياء؛ يبدأ lookup/reconcile أولًا.

## أقسام لوحة التحكم المرتبطة — التحديد التنفيذي الإلزامي

| القسم والمسار | الصفحة/التبويب | ما يظهر والتحكمات | الحالات والصلاحيات | الاختبار |
|---|---|---|---|---|
| **المنصة** `/dsh/platform` | Provider Detail → connection/breaker/webhook/retry diagnostics | timeout budgets، error classes، breaker state، last probe، signature versions، DLQ؛ test/maintenance/reconcile | platform/provider permissions؛ CLOSED/OPEN/HALF_OPEN/DEGRADED/UNKNOWN | DNS/TLS/timeout/429/5xx/4xx، half-open/recovery، signature rotation |
| **الرئيسية** `/dsh/dashboard` | breaker/outage alerts | impacted capabilities، state/freshness/owner، retry backlog | open Platform/Incident فقط | aggregate masked؛ no blind retry | افتح breaker وتحقق من alert والتعافي |
| **العمليات** `/dsh/operations` | provider-dependent Order/Dispatch/Tracking actions | degraded/unknown reason، last safe state، allowedActions | retry UI حسب classification أو lookup؛ no direct provider replay | operations permissions؛ pending/unknown/unavailable | provider success after local timeout، duplicate action، recovery |
| **المالية** `/wlt/finance` | Payment/Payout/Reconciliation provider attempts | provider refs/events masked، result lookup، signature/replay state | verify/reconcile/retry safe فقط بعد lookup، no force-success | WLT finance permissions؛ unknown/late-success/replayed | timeout ثم late success، webhook duplicate/invalid، no duplicate money movement |
| **الدعم** `/dsh/support` | Provider incident/escalation links | customer-safe case status، internal diagnostics link | open/escalate incident؛ لا provider mutation من ticket | support scope/internal visibility | أنشئ ticket أثناء outage وتحقق من عدم كشف raw errors/IDs |

Apps تعرض feature-level degradation فقط؛ لا raw provider error أو breaker control.

## التقنية

- connect/read/overall timeouts.
- retry فقط للأخطاء المؤقتة والعمليات الآمنة/idempotent.
- breaker thresholds وhalf-open probes.
- webhook signature/timestamp/replay protection.
- inbox deduplication وlate/out-of-order handling.
- correlation من Surface إلى provider.

## الاختبارات

- DNS/TLS/timeout/429/5xx/4xx.
- retry exhaustion وstorm prevention.
- breaker open/half-open/recovery.
- invalid/replayed/rotated webhook signatures.
- provider success after local timeout.
- duplicate webhook.
- all CP controls use lookup/classification before retry.

## التجريب اليدوي

1. حاكي كل فئة فشل عبر simulator.
2. راقب Platform وDashboard وOperations وFinance وSupport.
3. نفّذ retry من Surface وتأكد من عدم storm.
4. أرسل webhook مكررًا وخاطئ التوقيع ومتأخرًا.
5. اختبر unknown result ثم reconcile.
6. تحقق من logs/metrics والتعافي.

## الإغلاق

`unbounded_provider_calls=0; unsafe_retries=0; unsigned_or_replayed_webhooks=0; duplicate_provider_effects=0; breaker_recovery=PASS; unmapped_control_panel_sections_or_controls=0; evidence_sha=CURRENT_SHA`.
