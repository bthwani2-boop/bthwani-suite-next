# J048 — تزامن السلة واستعادتها

```yaml
journey_id: J048
owner: DSH
actors: [CUSTOMER, OPERATOR, SERVICE_IDENTITY]
surfaces: [app-client, control-panel]
control_panel_sections: [operations, support, administration, platform, analytics]
dependencies: [FOUNDATION-00, J047]
assessment_status: NOT_ASSESSED
closure_status: OPEN
execution_order: STRICT_SEQUENTIAL
```

## النتيجة التشغيلية النهائية

تستعاد السلة نفسها عبر الأجهزة والجلسات ضمن Actor واحد، مع merge policy معلنة عند guest/auth أو offline/online. الخادم هو الحكم، والعميل يحتفظ cache مؤقتًا مع version ولا يخلق سلة ثانية أو يفقد تعديلًا بصمت.

## تشخيص الحالة الحالية

### الموجود
- session/device management في Identity.
- Cart domain في DSH وmobile runtime متعدد الأسطح.

### الناقص
- guest cart policy وهل هي مطلوبة أصلًا.
- merge algorithm وconflict UX.
- resume token/version بعد reinstall/login.
- offline queue limits/order/deduplication.
- expiration policy وإشعارات المستخدم.
- اختبار multi-device runtime حقيقي.

### الخاطئ أو الممنوع
- last-write-wins صامت بين الأجهزة.
- دمج quantities أو options دون قواعد موثقة.
- إرسال queue قديمة بعد تحول السلة إلى order.
- استخدام device storage كمصدر دائم.
- نجاح مزعوم أثناء offline دون pending state.

### التصحيح المطلوب
- server sync protocol مع version/cursor.
- merge command idempotent ونتيجة تفصيلية.
- invalidation بعد conversion/expiry/logout حسب السياسة.
- frontend sync controller واحد بدل منطق موزع.

### الإضافات المطلوبة
- شاشة conflict/merge summary عند الحاجة.
- sync status وlast updated.
- diagnostics للأجهزة والqueues دون كشف PII.

## منطق التشغيل

- عند startup/login: fetch authoritative cart ثم reconcile pending commands.
- الأوامر القديمة ذات base version منتهية تعود conflict ولا تطبق عميانيًا.
- guest-to-auth merge إن كان معتمدًا ينتج سجلًا واحدًا وmerge report.
- بعد `CONVERTED` ترفض كل queued mutations القديمة.
- expiration لا تحذف evidence المطلوبة وتظهر للمستخدم بوضوح.

## App Client

- sync indicator غير مزعج، pending badge عند offline، conflict sheet عند اختلاف غير آمن.
- controls: `مزامنة الآن`، `مراجعة التغييرات`، `استخدام نسخة الخادم`، `إعادة تطبيق عناصر صالحة`.
- لا يظهر cart badge خاطئ بعد login/logout/reinstall.
- حالات: offline pending، stale، merging، conflict، expired، converted.

## أقسام لوحة التحكم المرتبطة — التحديد التنفيذي الإلزامي

| القسم والمسار | الصفحة/التبويب | البيانات والتحكمات | الحالات والصلاحيات | الاختبار |
|---|---|---|---|---|
| **العمليات** `/dsh/operations` | Cart Sync Diagnostics | authoritative cart/version، devices/session refs، pending/replayed command IDs، merge report، expiry/conversion | trigger readback/revalidate/invalidate corrupt sync token/open incident؛ no choose customer version silently | operations diagnostics permission؛ synced/pending/merging/conflict/expired/converted/unknown | two devices، queue after conversion، duplicate replay، stale base |
| **الدعم** `/dsh/support` | Customer Ticket → Sync/Conflict assistance | masked device/cart states، rejected commands/reasons، customer-selected resolution status | request info/open diagnostics/resend safe instruction؛ no replay privileged command | support object/purpose scope؛ waiting/conflict/resolved | Actor switch، reinstall، offline queue، cross-customer leakage |
| **الإدارة** `/dsh/administration` | Actor Detail → Sessions/Devices | session/device identity/status and Cart sync reference only | revoke device/session/open Operations؛ no Cart merge or line mutation | Identity permissions؛ active/revoked/compromised | logout/login Actor different on same device، lost device revocation |
| **المنصة** `/dsh/platform` | Offline/Cache/Jobs/Event diagnostics | sync protocol version، queue/job health، cache/checkpoints، cleanup/retention | reconcile/rebuild/open incident؛ no Cart truth edit | platform diagnostics؛ degraded/rebuilding/error | stop sync worker، event gap، cleanup and recovery |
| **التحليلات** `/dsh/analytics` | Sync/conflict/offline metrics | conflict rate، recovery time، expired queue counts، source/freshness، no item/PII detail | filters/drill-down/export scoped | analytics privacy permission | compare diagnostics، duplicate event and small-sample suppression |

Finance/Partners/Catalogs do not resolve sync conflicts; they emit invalidation events or consume final Cart state only.

## التقنية والبيانات

- command IDs مستقرة، ordered queue، per-actor partition.
- merge rules deterministic وunit-tested.
- secure local storage للحد الأدنى دون PII زائد.
- telemetry لا ترسل محتوى الملاحظات أو items كاملة.
- retention للqueues/idempotency records.

## الاختبارات الآلية

- جهازان يعدلان quantity/options.
- offline queue ثم server change.
- logout/login Actor مختلف على الجهاز نفسه.
- reinstall/resume.
- guest merge إن كان منطبقًا.
- queue بعد order conversion.
- expiry boundary وclock skew.
- duplicate replay.
- section visibility/action mapping.

## التجريب اليدوي

1. افتح الحساب على جهازين وعدّل السلة بالتزامن.
2. افصل أحد الجهازين، نفّذ عدة أوامر، عدّل الآخر، ثم أعد الاتصال.
3. جرّب logout إلى Actor آخر وتحقق من Administration وعدم تسرب السلة.
4. أعد تثبيت التطبيق أو امسح cache ثم سجّل الدخول.
5. أنشئ Order بينما جهاز آخر offline ثم أعده.
6. افحص Operations/Support/Platform/Analytics وmerge/conflict UI.

## بوابة الإغلاق

`cross_actor_cart_leakage=0; silent_last_write_wins=0; post_conversion_replay=0; deterministic_merge=PASS; multi_device_runtime=PASS; offline_queue_recovery=PASS; unmapped_control_panel_sections_or_controls=0; evidence_sha=CURRENT_SHA`.
