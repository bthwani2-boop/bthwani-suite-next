# Claude Code Adapter

This file is routing only. It owns no repository, Product, architecture, execution, deletion, migration, CI, compatibility or closure semantics.

Canonical repository execution/closure authority for branch `h`:

`tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`

For any material repository work on `h`:

1. Resolve the exact live remote `h` SHA.
2. Load `00-ORCHESTRATOR.md`, `01` through `05`, and every materially applicable `focus/*` semantic owner.
3. Reconstruct the current campaign stage and any `ACTIVE_OPEN_UNIT` / `RECOVERY_FRONTIER` before selecting work.
4. Execute only under the orchestrator's Stage-A/Stage-B, refoundation, migration/cutover, deletion/pruning, verification and closure laws.
5. Treat `governance/**` as inherited Product/System/policy truth input that must be reconciled under the orchestrator; it is not an execution-order, authority-resolution, artifact-survival or closure controller.
6. Load `.agents/**`, tool adapters, Product Truth and governance material only when materially required by the active unit.
7. Any Claude/Google delegation is subordinate to the same single controlling execution session and may not create a competing coordinator, mutation authority or completion claim.
8. Planning artifacts, tool output, agent memory and historical governance are evidence only unless the orchestrator's truth-reconciliation and verification laws establish the material claim on current `h`.

If this adapter conflicts with the canonical orchestrator, the orchestrator wins and this adapter must be corrected or deleted.
