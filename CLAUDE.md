# BThwani Suite Next — Claude Code Entry

Read first:

1. `AGENTS.md`
2. `.agents/INDEX.md`
3. `.agents/AUTHORITY_BOUNDARY.md`
4. `.agents/adapters/claude.md`
5. One or two relevant `.agents/skills/*/SKILL.md` files only

Rules:

- Do not create tool-specific rule mirrors.
- Do not restore realtest donor trees.
- Do not widen scope.
- Use `C:\bthwani-suite-next` as the local root.
- Use PowerShell and `pnpm`; never use `npx`.
- Treat Graphify as a context/navigation tool only.
- Do not claim `PASS`, `READY`, `CLOSED`, `FINAL`, or `100%` without evidence.
