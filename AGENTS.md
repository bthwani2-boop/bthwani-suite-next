# BThwani Agent Adapter

This file is routing only. It is not a repository truth source and must not restate or weaken the canonical refoundation law.

Canonical repository execution/closure authority:

`tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`

For repository work on branch `h`:

1. Resolve the exact live remote `h` SHA.
2. Load `tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`.
3. Load `01`–`05` and every materially applicable `focus/*` owner.
4. Follow the orchestrator's repository-wide forensic census → required-truth extraction → CURRENT/CANONICAL modeling → dynamic causal graph → highest correct execution-unit selection → refoundation → migration/cutover → deletion/pruning → admission-hole closure → exact-h verification/falsification → re-diagnosis loop.
5. The execution unit may be broad; do not shrink it to a file/bug/session when the proven root is a package family, service, domain, top-level surface, migration epoch, contract lineage, runtime/config surface, assurance control plane, or governance/tools/docs surface.
6. Treat `.agents/**`, `.github/**`, `.opencodereview/**`, `docs/**`, `tools/**`, `governance/**`, CI, scanners, historical plans/prompts and old branches as inherited structure/evidence only. None is a preservation authority.
7. Never redefine scope, execution-unit ranking, artifact survival, mutation authority, deletion law, compatibility law, CI authority or closure semantics here.

For the `h` campaign, direct fast-forward mutation on `h` is authorized by current human intent. PR/merge/rebase/force-push behavior is governed only by the orchestrator.

If any text in this adapter conflicts with the orchestrator, the orchestrator wins.
