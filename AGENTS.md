# BThwani Agent Adapter

This file is routing only. It is not a repository truth source and must not restate or weaken the canonical refoundation law.

Canonical repository execution/closure authority:

`tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`

For repository work on branch `h`:

1. Resolve the exact live remote `h` SHA.
2. Load `tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`.
3. Load `01`–`05` and all materially applicable `focus/*` owners.
4. Follow the orchestrator's full-repository forensic census, Root Graph, refoundation, migration, cutover, deletion, pruning, verification and re-diagnosis loop.
5. Treat `.agents/**`, tools, CI, scanners, historical plans, prompts and old branches as adapters/evidence only.
6. Never redefine scope, root ranking, artifact survival, mutation authority, deletion law, compatibility law, CI authority or closure semantics here.

For the `h` campaign, direct fast-forward mutation on `h` is authorized by current human intent. PR/merge/rebase/force-push behavior is governed only by the orchestrator.

If any text in this adapter appears to conflict with the orchestrator, the orchestrator wins.
