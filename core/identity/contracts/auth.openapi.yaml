openapi: 3.1.0
info:
  title: BThwani Core Identity API
  version: 0.1.0
  description: Authentication, session verification, actor identity, roles, and permissions.
x-bthwani-contract-state: CONTRACT_DRAFT
x-bthwani-owner: core/identity
paths:
  /auth/session:
    get:
      operationId: getAuthSession
      summary: Resolve current actor session.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Authenticated actor session.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "401":
          description: Missing or invalid session.
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  schemas:
    AuthSession:
      type: object
      required: [authState, subject, roles, permissions]
      properties:
        authState:
          type: string
          enum: [authenticated]
        subject:
          type: string
          minLength: 1
        roles:
          type: array
          items:
            type: string
            enum: [client, partner, captain, field, operator, system]
        permissions:
          type: array
          items:
            $ref: "#/components/schemas/Permission"
    Permission:
      type: object
      required: [service, surface, action, scope]
      properties:
        service:
          type: string
          enum: [dsh, wlt, knz, arb, amn, esf, mrf, snd, kwd, core]
        surface:
          type: string
          enum: [app-client, app-partner, app-captain, app-field, control-panel, webapp, website, system]
        action:
          type: string
        scope:
          type: string
          enum: [own, assigned, store, zone, all, none]