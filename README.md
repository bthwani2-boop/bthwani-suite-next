# bthwani-suite-next

Active BThwani implementation repository.

## `h` canonical-baseline campaign

Branch `h` is the only mutable world for the current canonical-baseline refoundation campaign. Its repository execution and closure constitution is:

`tools/prompting/bthwani-orchestrator/00-ORCHESTRATOR.md`

Load `00` through `05` and the materially applicable `focus/*` owners before material `h` work. Recover from live `h`; do not select new work from chat memory or historical plans.

The campaign is mandatory two-stage:

```text
STAGE_A = SYSTEMIC BASELINE DECONTAMINATION
STAGE_B = NORMAL DYNAMIC ROOT CLOSURE
```

Stage B is forbidden until the current exact `h` passes the Stage-A exit gate.

## Evidence and truth inputs

No tracked path is automatic truth or receives preservation rights because it exists. Material meaning is reconciled under the orchestrator from current human intent, required Product/System/Data/Security truth, live implementation/contracts/data/runtime evidence, authoritative external evidence when needed, and historical/forensic support.

Useful current inputs include:

- Product/System inputs: `governance/product/**`
- Policy inputs: `governance/policies/**`
- DSH implementation: `services/dsh/**`
- WLT implementation: `services/wlt/**`
- Cross-cutting domains: `core/**`
- Application runtimes: `apps/**`
- Reusable platform/runtime packages: `shared/**`
- Executable assurance: `.github/**`, `tools/**`, tests and runtime probes
- Exact repository-platform state: query GitHub live for the target branch and candidate SHA

Every one of these surfaces must re-earn its canonical owner/container role during refoundation. Governance, code, CI, tests and current paths may contradict one another; contradictions are diagnosis evidence.

## Structural rule

Preserve required truth, not inherited topology. Choose the highest proven causally complete execution unit permitted by the current campaign stage. Migrate/cut over completely, delete losing authorities and containers at the highest safe canonical granularity, recursively prune residue, close the admission hole, then verify and falsify negative space.

```text
GREEN != CANONICAL
COMMIT != CLOSED_UNIT
CHECKPOINT != COMPLETION
```

Only the canonical orchestrator may emit the official Level-4 completion token after Stage A, Stage B and the final fresh adversarial repository qualification all pass on the final exact `h` SHA.
